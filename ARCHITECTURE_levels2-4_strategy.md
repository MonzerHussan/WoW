# قرار معماري — استراتيجية بناء المستويات 2-4 (WOW)

> وثيقة قرار، مش برييف تنفيذ بعد. تُلصق كمرجع دائم، ويُبنى عليها كل TASK
> بريف لاحق للمستويات 2، 3، 4. مبنية على مراجعة المستندات الأربعة
> المرفوعة: `WOW_Level2_Bilingual.docx`, `WOW_Level3_Bilingual.docx`,
> `WOW_Level4_Bilingual.docx` (+ نسخهم الإنجليزية)، بعد نقاش مع Monzer
> بتاريخ 2026-08-06.

---

## 1. السياق: مش أربع مهام منفصلة

المستويات الأربعة بتشترك في أنظمة متكررة، بتكبر من مستوى للتاني:

| النظام | يبدأ في | يتوسع في |
|---|---|---|
| ذاكرة الشخصيات (Character Memory) | مستوى 3 (Sarah وزملاء الفريق) | مستوى 4 (Board Memory — نفس الفكرة على مجلس إدارة) |
| الـDNA المتراكم | Career DNA (1-2) | Leadership DNA (3) → Executive DNA + Ethical Judgment (4) |
| السرد بالذكاء الاصطناعي (Project Story) | مستوى 2 | Leadership Report + Lessons Learned (3) → Portfolio Story + دمج قصص 1-4 (4) |
| محرك السيناريوهات الديناميكي | Final Boss، 9 سيناريوهات عشوائية (2) | Mega Delivery Simulation (3) → Enterprise Transformation Simulation (4) |
| تصدير Portfolio/PDF | Portfolio PDF بسيط (2) | 8 ملفات قابلة للتصدير (3) → Integrated Executive Portfolio يدمج 1-4 (4) |

**القرار**: تُبنى هذه الأنظمة كبنية تحتية مشتركة قابلة لإعادة الاستخدام،
أول ظهور فعلي ليها في مستوى 2 بنسخة بسيطة — مش تُبنى من الصفر في كل
مستوى.

---

## 2. الخطر الأكبر: تصعيد تكلفة LLM عبر المستويات

كل مستوى متكئ بشكل تصاعدي على استدعاءات LLM حقيقية (فلوس حقيقية لكل
استدعاء)، وأمثلة ملموسة من المستندات نفسها:

- **Final Boss (مستوى 2)**: المستند صراحة بيقول إعادة المحاولة "تولّد
  مجموعة سيناريوهات جديدة" — بلا أي سقف أو تسعير مذكور. لو بُنيت حرفيًا،
  فتحنا نفس ثغرة "مدد بلا حدود" اللي واجهناها مع المكالمة الصوتية
  (migration 036)، بس من غير حتى الحماية الجزئية اللي بنيناها هناك.
- **Triple Evaluation (مستوى 3)**: Rafeeq بيقيّم 30% من **كل قرار**، مش
  بس نهاية المستوى — استدعاءات متكررة طول الرحلة.
- **ذاكرة الشخصيات/المجلس (3، 4)**: عشان الشخصية "تفتكر" تاريخها معاك،
  إما نبعت التاريخ الكامل مع كل استدعاء (تكلفة متصاعدة كل ما اللعب طال)،
  أو نبني نظام تلخيص — بند هندسي لازم يتقرر من الأول.
- **دمج القصص (مستوى 4)**: استدعاء واحد لازم يقرأ ملخص 4 مستويات كاملة
  عشان يكتب قصة واحدة.

هذا تصعيد أضخم بكتير من أي حاجة بُنيت لحد الآن على المنصة.

---

## 3. القرار المعماري: Knowledge-Base-first، مش LLM-first

**المبدأ**: أي تقييم مساحة إجاباته محدودة ومعروفة مسبقًا (اختيار من
متعدد، مسارات قرار محددة) **يُبنى كقاعدة معرفة (Knowledge Base) مُصمَّمة
مسبقًا من فريق المحتوى** — جدول قواعد "سيناريو × قرار = درجة + سبب +
ملاحظة"، يُطبَّق بكود حتمي بلا أي استدعاء LLM. الاستدعاء الحي لـLLM
يُحجز فقط للحالات المستحيل بناؤها كقاعدة معرفة (توليد نص سردي جديد لكل
متعلم، حوار طبيعي غير متوقع).

**مثال مُفصَّل — Final Boss (مستوى 2)**: المستند نفسه بيقسّم التقييم
لتلات معايير:
1. **فعالية الحل** — لو اختيار من متعدد، تصحيح آلي حتمي (زي أي اختبار
   حالي، مفتاح إجابة معزول).
2. **السرعة** — توقيت رقمي بحت، بلا أي علاقة بذكاء اصطناعي.
3. **التوازن** — الوحيدة اللي المستند بنفسه سمّاها "منطقة رمادية يقيّمها
   Rafeeq"، ولو بس الإجابة نص حر.

**النتيجة**: لو التزمنا بإجابات اختيار من متعدد لـFinal Boss (مساحة
9 سيناريوهات × قرارات محدودة = تركيبات معدودة رياضيًا)، **نلغي الحاجة
لـLLM في هذه الميزة بالكامل** — تقييم فوري، حتمي، بلا أي تكلفة متكررة،
بلا معضلة فوترة من الأساس. التكلفة الحقيقية هنا جهد تصميم محتوى لمرة
واحدة (كتابة كل تركيبة ودرجتها)، بنفس نمط تأليف سيناريوهات الألعاب
والنقاط القواعدية الموجود فعليًا في المنصة — مش تكلفة مالية متكررة.

**بديل للإجابات النصية الحرة (لو لزم)**: بدل LLM حي، نستخدم نفس نمط
الاختبارات الهجينة الموجود فعليًا (تسجيل + اعتماد مقيّم بشري) — بنية
مُختبرة أمنيًا من قبل، بلا أي تكلفة OpenAI إضافية.

---

## 4. تحليل النقاط الأربع المعلّقة — مكتمل (2026-08-06)

طُبِّق نفس تحليل "هل تتحول لقاعدة معرفة؟" على كل نقطة على حدة:

| النقطة | الخلاصة | السبب |
|---|---|---|
| Triple Evaluation's Rafeeq (30%، مستوى 3) | **قابلة لقاعدة معرفة** | نفس منطق Final Boss بالضبط — ألعاب زي Sprint Commander مبنية على نقاط قرار محددة (زي كل ألعاب المستوى الأول الحالية)، مش نص حر. |
| ذاكرة الشخصيات/Board Memory (3، 4) | **الأرجح قابلة لقاعدة معرفة، بشرط واحد معلّق** | الذاكرة نفسها (ثقة/احترام/توتر) بيانات محضة تتحدّث بقواعد بسيطة — صفر LLM. الملاحظات النصية يمكن تكون قوالب مرتبطة بأحداث محددة، **طالما التفاعل نقاط قرار مش شات حر مفتوح** — هذا الافتراض الوحيد يحتاج تأكيد وقت تصميم اللعبة الفعلي. |
| التقييم الأخلاقي (مستوى 4) | **قابلة لقاعدة معرفة، بلا أي غموض** | أوضح حالة — قرار ثنائي واضح بنتيجة معروفة مسبقًا (مثال الرشوة في المستند نفسه). |
| القصة السردية (Project Story) | **قابلة لقاعدة معرفة أيضًا — اكتشاف جديد** | قالب مستوى 2 نفسه ("تم تقسيم العمل إلى [عدد] حزم...") **قالب جاهز بفراغات فعليًا في المستند المصدر** — ملء متغيرات من بيانات اللعب الحقيقية بدل استدعاء LLM كامل. |

**النتيجة الإجمالية**: كل نقاط التقييم الموثَّقة حاليًا في المستويات 2-4
تُبنى كقواعد معرفة بلا أي تكلفة LLM متكررة، **ما عدا نقطة واحدة مفتوحة**:
لو تصميم اللعبة الفعلي لذاكرة الشخصيات يحتاج محادثة حرة حقيقية (مش نقاط
قرار)، وقتها فقط يحتاج LLM حي — يُحسم وقت تصميم تلك اللعبة تحديدًا، مش
الآن.

**القرار المعماري النهائي**: حوكمة التكلفة اللي كانت مطروحة في §2 أصبحت
**غير مطلوبة عمليًا** في أغلب النطاق — لأن مفيش استدعاءات متكررة أصلًا
تحتاج حوكمة. تصميم التسعير/الحوكمة يُؤجَّل لحين ظهور حاجة LLM حي فعلية
(في تصميم لعبة ذاكرة الشخصيات لاحقًا)، بدل ما يُبنى مقدَّمًا لسيناريو
مش هيحصل.

**التالي**: كتابة أول TASK بريف تنفيذي — البنية التحتية المشتركة (الـDNA
الموحّد، محرك السيناريوهات القابل لإعادة الاستخدام، خدمة السرد المبنية
على قوالب) + محتوى مستوى 2 الدراسي.
