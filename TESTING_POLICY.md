# TESTING_POLICY.md — سياسة الاختبار المستمر (ملزمة)

## المبدأ الأساسي

**لا يُعتبر أي Sprint "مكتملًا" حتى يجتاز اختبار قبول (Acceptance Test)
موثّقًا — بغض النظر عن مدى اكتمال الكود.** هذا ينطبق بأثر رجعي على كل ما
بنيناه، وبشكل إلزامي على كل ما يُبنى بعد هذا الملف.

## لماذا هذا ملزم لا اختياري

اكتشفنا عمليًا (اختبار المستخدم اليدوي لصفحة التسجيل) أن الكود "الصحيح
نظريًا" قد يُخفي فجوات حقيقية (مثل: التنسيق المفقود، لوحة معهد تعرض واجهة
طالب). الاختبار اليدوي بعد كل مرحلة هو ما كشف هذه الفجوات — وليس مراجعة
الكود وحدها.

## ما تغطيه الاختبارات الآلية — وما لا تغطيه (صريح)

منذ إضافة CI، يعمل على كل push إلى `main` وكل PR إليه:
`npm ci` → `tsc --noEmit` → `npm run lint` → `npm run test` → `npm run build`.
فشل أي خطوة يوقف الدمج.

### ما تحميه الاختبارات الآلية فعلاً

- **مخططات zod** — مكتوبة حول ما **ترفضه** لا ما تقبله: سقوف التاريخ
  المحفوظ لدى العميل، رفض المعرّفات غير الصالحة، و**تجريد أي حقل مدسوس**
  (`amount`، `coinCost`، `score`، `cap`). لاحظ أن zod يُسقط المفاتيح
  المجهولة بدل رفض الطلب، فالخاصية الأمنية ليست فشل التحليل بل **غياب
  الحقل من الناتج** — وهذا ما تؤكده الاختبارات.
- **`resolveLanguageTask`** — أي مفتاح تسعير يُختار لكل شكل من شكلَي
  المهمة، **وأن `coin_cost` المدفون في المحتوى يُتجاهل تماماً** (جوهر 024).
- **`clip`** — سقوف اقتطاع سياق الدرس. كانت TECH_DEBT #19 تسجّل أن هذا
  الفرع **لم يُشغَّل قط على بيانات حقيقية** لأن كل الدروس أقصر من السقف
  بكثير؛ الاختبار يغطيه الآن مباشرة.
- **`selectNewBadges`** — منطق العتبة الشامل الذي حلّ محل نافذة الفرق.
- **تطابق قيم النقاط بين TypeScript وSQL** — يقرأ
  `027_points_award_hardening.sql` فعلياً ويقارن ثوابته بـ`REASON_POINTS`.
  كلا الملفين يحمل تعليقاً يقول «حافظ على تزامنهما يدوياً» — وهو بالضبط
  نوع التعليمات التي تُنسى. الآن يفشل CI بدل أن يُدفع للمتعلّمين مبلغ خاطئ
  بصمت.
- **نظافة الـmigrations** — لا رقمان متطابقان، ولا migration بعد 028
  يعيد `correct_index` إلى الـjsonb (الثغرة التي أُغلقت هناك).

### 🔴 ما لا تغطيه — ويحتاج اختباراً يدوياً حياً

**سياسات RLS ودوال `security definer` — أي بالضبط فئة الثغرات الأربع التي
أُغلقت في 025-029 — غير مغطاة إطلاقاً بالاختبار الآلي.**

CI **لا يلمس قاعدة بيانات حية**، ومتغيرات البيئة فيه قيم وهمية. وهذا
**قرار مقصود لا نقص**: لا يوجد مشروع Supabase اختباري منفصل عن الإنتاج،
واختبار RLS على الإنتاج غير وارد.

فلا شيء آلي يكشف:
- سياسة RLS مفقودة أو واسعة أكثر من اللازم
- دالة `security definer` كُتبت بحيث لا تحرس شيئاً (**هذا ما حدث حرفياً في
  025** — راجع القسم التالي)
- رفض صامت يعيد 200 بدل 403 (TECH_DEBT #24)
- أي تسريب بيانات عبر PostgREST يتجاوز مسارات التطبيق

**كل إصلاح يمسّ هذه الطبقة يبقى تحت قاعدة «أعد تشغيل الهجوم» أدناه، بحساب
حقيقي وأدلة حقيقية.** لا تقرأ نجاح CI على أنه تغطية أمنية.

**مهمة مستقبلية منفصلة (غير مجدولة)**: مشروع Supabase اختباري مستقل عن
الإنتاج، تُشغَّل عليه الـmigrations آلياً وتُختبر السياسات بحسابات وهمية.
حتى ذلك الحين، تشغيل الـmigrations يدوي كما كان طوال المشروع — عمداً.

## قاعدة إلزامية: إصلاح أمني يُختبر بإعادة تشغيل الهجوم نفسه

**كل إصلاح لثغرة أمنية لا يُعتبر مغلقًا حتى يُعاد تشغيل الهجوم الأصلي
حرفيًا ضد الإصلاح، بأدلة حقيقية: رمز HTTP الفعلي، وقراءة مستقلة من قاعدة
البيانات بعد الرفض.** مراجعة الكود وحدها لا تكفي، ولا يكفي أن يعيد المسار
رسالة خطأ — رمز الخطأ لا يثبت أن الكتابة لم تحدث.

الحد الأدنى لكل إصلاح:

1. **خط أساس مُثبَّت قبل الإصلاح** — شغّل الهجوم ووثّق نجاحه (رمز الحالة
   والقيم التي تغيّرت فعلًا). بلا خط أساس، لا يوجد ما تُنسَب إليه النتيجة
   بعد الإصلاح.
2. **نفس الهجوم بعده، بنفس الحساب ونفس الأمر** — فيكون الفرق منسوبًا
   للإصلاح وحده لا لتغيّر في الظروف.
3. **قراءة مستقلة بعد الرفض** — تأكيد أن القيم لم تتغيّر، لا الاكتفاء
   برمز الخطأ.
4. **اختبار عدم الانحدار** — أن المسار المشروع ما زال يعمل. إصلاح يوقف
   الهجوم ويكسر الاستخدام العادي ليس إصلاحًا.
5. **دليل إيجابي حين يكون الادعاء "لم يحدث شيء"** — غياب سطر في السجل
   ضعيف بمفرده. مثال مطبَّق: لإثبات أن استدعاء OpenAI لم يقع، قِس زمن
   الاستجابة بعد إحماء المسار — ثبات ~1.4 ثانية (رحلة قاعدة البيانات
   وحدها، أقل بكثير من زمن النموذج) دليل إيجابي، لا مجرد غياب سجل.

### المثال الذي فرض هذه القاعدة: migration 025

`025` كُتب لإغلاق ثغرة تصعيد صلاحيات (أي مستخدم يمنح نفسه `admin` بطلب
PATCH واحد). الملف بدا صحيحًا تمامًا في المراجعة، وشُغِّل بنجاح، وأكّد
فحص `pg_trigger`/`pg_proc` أن الـtrigger موجود.

**وكان معطَّلًا بالكامل.** دالة الحارس كُتبت بـ`SECURITY DEFINER`، وداخل
دالة كهذه تُرجع `current_user` **مالك الدالة** لا دور الجلسة — فالشرط
المبكر `if current_user not in ('authenticated','anon') then return new`
كان صحيحًا في كل استدعاء، والدالة تخرج قبل أي فحص. حارس صامت لا يحرس
شيئًا، ويبدو من الخارج مطابقًا تمامًا لحارس يعمل.

لم يكشفه فحص بنيوي ولا قراءة كود — كشفه فقط **إعادة تشغيل نفس الأربع
محاولات PATCH**: عادت جميعها `204` والأعمدة تغيّرت فعلًا. الإصلاح الحقيقي
كان `026`. بعده أعيد نفس الاختبار: `403` مع `42501`، والقراءة المستقلة
أثبتت أن القيم لم تتغيّر.

**الخلاصة**: `SECURITY DEFINER` ضرورية لدالة **تتصرّف** بصلاحيات مرفوعة،
وضارّة لدالة **تفحص من يتصرّف**. ولولا إعادة تشغيل الهجوم لَوُثِّقت ثغرة
مفتوحة على أنها مغلقة — وهو أسوأ من عدم إصلاحها، لأنه يزيل الحذر.

## بوابة القبول لكل Sprint (Definition of Done الموسّع)

كل Sprint يُغلق فقط بعد اجتياز الخطوات الثلاث التالية بالترتيب:

### 1. اختبار وظيفي يدوي (Functional Walkthrough)
المالك (أو مختبِر) يمر يدويًا على كل ميزة جديدة بالسيناريو الطبيعي
والسيناريو الفاشل عمدًا (بيانات خاطئة، إلغاء منتصف عملية، رابط غير
موجود). يُوثَّق بلقطات شاشة أو وصف نصي لكل حالة + النتيجة (نجاح/فشل).

### 2. فحص الوثائق (Docs Sync Check)
هل تغيّر أي شيء معماري/أمني/في قاعدة البيانات؟ إن نعم، هل تحدّثت
الوثيقة المقابلة (ARCHITECTURE.md / SECURITY.md / DOMAIN_CONTRACTS.md /
ROADMAP.md) في نفس الدفعة؟ الوثائق المتأخرة عن الكود = دين تقني، وتُمنع.

### 3. فحص بناء نظيف (Clean Build Check)
`npm run build` و`npm run lint` و`npm run test` تمر بلا أخطاء على نسخة
حديثة مسحوبة من GitHub (لا نسخة محلية قد تحوي إصلاحات غير مرفوعة).

**لا ننتقل للـ Sprint التالي إلا بعد اجتياز الثلاثة.**

## خطة اختبار Beta المصغّر (بعد اكتمال Sprint 3)

هدفها: كشف مشاكل الأداء والاستخدام الحقيقي قبل أي استثمار في سعة إضافية
(Redis، ترقية Supabase) — نقيس أولًا، ثم نطوّر بناءً على بيانات حقيقية لا
تخمين، بنفس فلسفة تأجيل الاشتراكات سابقًا.

### النطاق
- 5–10 مستخدمين حقيقيين (أصدقاء/عائلة)، خليط من الفئات (طالب، باحث عن
  عمل، موظف على الأقل)
- مدة أسبوع واحد
- بيئة: نفس Supabase الحالي (الخطة المجانية) — لا ترقية مسبقة

### معايير النجاح (Success Criteria) — محدّدة رقميًا لا انطباعيًا
| المقياس | الحد المقبول |
|---|---|
| نجاح التسجيل الكامل (تسجيل→onboarding→dashboard) | 100% من المختبرين بلا مساعدة يدوية مني |
| زمن استجابة Dashboard | أقل من 3 ثوانٍ على اتصال عادي |
| أخطاء غير معالجة (رسالة تقنية خام تظهر للمستخدم) | صفر حالات |
| نجاح إكمال درس واحد + ظهور النقاط فعليًا | 100% |
| استقرار محادثة الوكيل (بلا انقطاع 502/timeout) | ≥ 90% من المحاولات |

### ماذا نفعل بالنتائج
- كل مشكلة تُسجَّل كسطر في TECH_DEBT.md بدرجة خطورة
- أي فشل في معيار النجاح = يُحل قبل دعوة دفعة Beta أوسع، وليس قبل أي
  ميزة جديدة أخرى
- بعد اجتياز المعايير: ننتقل بثقة لتوسيع القدرة (Redis/ترقية Supabase)
  بأرقام حقيقية تبرر القرار، لا تخمينًا

## سجل الاختبارات (يُحدَّث بعد كل Sprint)

| Sprint | تاريخ الاختبار | النتيجة | ملاحظات |
|---|---|---|---|
| ما قبل Sprint 1 (التسجيل الأساسي) | ✅ اختُبر يدويًا | نجح | 7 فئات حساب + Google تعمل |
| التنسيق (postcss) | ✅ اختُبر يدويًا | نجح بعد إصلاح | كان معطلًا بالكامل أول مرة |
| Sprint 3 (LMS + الوكيل + البروفايل) | ✅ اختُبر بحسابين حقيقيين منفصلين (طالب + مقيّم) | نجح بعد 5 migrations إصلاحية إضافية (009-013) | كشف 9 أخطاء حقيقية (كوكيز، عرض المحتوى، النقاط، اختبار الدورة، PostgREST الغامض، RLS على entity_skills/skill_evidence/quiz_attempts/profiles/career_scores) — جميعها غير مكتشفة إلا باختبار حسابين حقيقيين لا حساب واحد. career_scores عند اعتماد مقيّم لمستخدم آخر: مؤجَّل بقرار واعٍ (TECH_DEBT). |
| النشر على الإنتاج (Vercel) | ✅ اختُبر بحسابين حقيقيين على رابط عام | نجح الحلقة الكاملة من أول محاولة بلا تدخل يدوي — يؤكد صلابة إصلاحات 010-013. بعد إضافة `OPENAI_API_KEY`: محادثة الوكيل اختُبرت فعليًا (`200 OK`، رد حقيقي من GPT-4o بسياق DNA صحيح، 7.44 ثانية — أقل من حد Vercel الافتراضي 10ث وضمن `maxDuration=30`) | career_scores عند اعتماد مقيّم لمستخدم آخر يبقى مؤجلاً بقرار واعٍ (البند الوحيد المتبقي). **كل معايير نجاح Beta الخمسة (قسم أعلاه) محقَّقة الآن فعليًا على الإنتاج.** |
| Beta المصغّر | ⬜ لم يبدأ | — | التالي فورًا بعد إغلاق Sprint 3 هذا |
