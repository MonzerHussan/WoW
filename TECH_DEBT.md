# TECH_DEBT.md

| # | Item | Severity | Where | Why it matters | Fix effort |
|---|---|---|---|---|---|
| 1 | Bilingual `COPY` dictionary duplicated 3x | Medium | signup, login, onboarding pages | Every new UI string has to be added in 3 places by hand; they will drift. | Small — extract to `constants/i18n/*.ts`, shared per feature. |
| 2 | No shared UI primitives (`Button`, `Input`, `Select`) | Medium | every form page | Styling/behavior (disabled states, focus rings) is copy-pasted; a design tweak means editing N files. | Small-Medium — build `components/ui/{Button,Input}.tsx` once shadcn/ui is initialized, migrate forms one at a time. |
| 3 | No request validation library | Medium | all `app/api/*` routes | Validation is ad hoc per route; easy to miss a check on a new route. | Small — introduce zod, one shared `schemas/` folder. |
| 4 | No custom data hooks (`useUser`, `useProfile`) | Low-Medium | signup/login/dashboard | Supabase calls are inlined per-component instead of centralized, making it harder to test or swap data sources later. | Medium. |
| 5 | `profiles` conflates identity + gamification state | Low | `supabase/schema.sql` | Works fine now; will matter if gamification logic grows complex enough to want its own table/service boundary. | Defer — revisit only if Sprint 6 (Gamification) needs it. |
| 6 | Automated tests cover pure logic only — **not** RLS or security-definer behaviour | Reduced from High to Medium | `tests/`, `vitest.config.ts` | 48 unit tests now guard the parts that can be tested without a database: every zod schema (written around what it must **refuse**, including that injected `amount`/`coinCost`/`score` fields are stripped from the parsed output), `resolveLanguageTask` (which pricing key each task shape selects, and that the `coin_cost` buried in lesson content is ignored — the point of 024), `clip` (the truncation caps that TECH_DEBT #19 recorded as never exercised), `selectNewBadges`, and a test that reads `027_points_award_hardening.sql` and asserts its SQL constants still match `REASON_POINTS` — turning a "keep these in sync by hand" comment into a CI failure. **What is still uncovered is the category that produced every real vulnerability in this project**: RLS policies and security-definer functions. CI touches no live database, by design — there is no test Supabase project separate from production. See TESTING_POLICY.md for the explicit list of what is and isn't covered. | Remaining work: a dedicated test Supabase project with migrations applied automatically and policies exercised by fixture accounts. Until then those fixes stay under the "replay the original attack" rule, verified by hand. **A SECOND consumer now waits on this same test project (2026-08-20):** the `smoke` gate (#39) can only run its public tier in CI, because CI builds with placeholder Supabase credentials and every protected route redirects without rendering — and the bug class that gate exists for lives behind auth. The full `npm run smoke` therefore runs locally before each commit instead. One test project would close both gaps; this is deliberately tracked here rather than opened as a separate item. |
| 7 | ~~No CI~~ **CI is live** — but it validates code, not database behaviour | Reduced from Medium to Low | `.github/workflows/ci.yml` | Every push to `main` and every PR into it runs `npm ci` → `tsc --noEmit` → `npm run lint` → `npm run test` → `npm run build`, and a failure blocks the merge. `npm ci` (not `npm install`) so a mismatched lockfile fails loudly instead of CI passing on a different dependency tree than the one committed. Supabase/OpenAI env vars are obvious placeholders and `WALLET_SIMULATION_ENABLED` is pinned to `"false"`, matching production — nothing in CI reaches a live service. The `ci/**` branch trigger exists so the pipeline itself can be validated on a scratch branch, which is how it was proven to actually fail on a broken test rather than assumed to. | Remaining: nothing for code. Database-layer verification needs the test project described in #6. Optionally add a required-status-check branch protection rule on `main` so the gate cannot be bypassed by an admin merge. |
| 8 | Route-based folder structure will not scale past ~Sprint 3-4 | Medium | whole repo | Documented with a full migration plan in `PROJECT_STRUCTURE.md` — intentionally not executed yet. | Large — scheduled as first task of Sprint 2. |
| 9 | ~~`career_scores(employability)` is not recomputed when an assessor confirms a human/hybrid quiz attempt for someone else~~ **CLOSED (migration 032)** | ~~Medium~~ resolved | `supabase/migrations/032_employability_recompute_on_grade.sql`, `app/api/lms/quizzes/grade/route.ts` | `recompute_employability_score(p_attempt_id)` — security definer, same shape as `award_quiz_points` (027): re-verifies `graded_by = auth.uid()` and the caller holds the `assessor` capability (copied from `award_quiz_points`'s own branch A predicate, not shared via a helper — no two definer functions in this codebase share one). Running RLS unaffected, it reads `entity_skills`/`quiz_attempts`/`lesson_progress` directly as its owner — checked first that the assessor's own session could **not** do this (broad `lesson_progress` read has no assessor policy at all; `entity_skills` assessor read is scoped to `source='assessment'` only), so no new read policy was opened anywhere. Called synchronously from `grade/route.ts` right after `award_quiz_points` succeeds. Always inserts a **new** `career_scores` row — no history touched. The TS auto-path function (`dna.service.ts`) is untouched; this is a second, parallel path for the assessor caller, same relationship as `award_lesson_points`/`award_quiz_points`. | Done. |
| 10 | Third-party badge grants (an org/system awarding a badge the user didn't trigger themselves) have no write path | Low | `user_badges` | `013`'s "owner inserts own" policy only covers the self-serve case already used by `points.service.ts`. Nothing currently needs third-party grants, so this is a real but currently-unused gap. | Small — same `security definer` pattern, when a real caller (e.g. an org awarding a badge) exists. |
| 11 | `profiles.age` is a static integer captured once at onboarding, not `date_of_birth` | Low (for now) | `profiles` (migration 016), onboarding wizard, agent context | The number freezes at whatever the user typed and never advances — a 27-year-old today is still "27" in the DB a year from now, silently drifting from reality everywhere it's read (agent context, any future age-based logic). Accepted as-is for this stage since exact age collection is itself a brand-new, deliberately provisional decision (RBAC.md "تحديث حرج على سياسة القاصرين"). | Small — swap the column for `date_of_birth date` and compute age at query time; straightforward migration since only onboarding writes it today. |
| 12 | `profiles.points` balance isn't shown anywhere on the lesson page itself | Low | `features/lms/components/LessonView.tsx` | The dashboard shows points, but a learner completing a lesson (and earning `LESSON_COMPLETE` points) has no in-page confirmation of their running total — only the one-off "+N points earned" toast from `LessonCompleteButton`. | Small — `LessonPage` already fetches the user; one more `profiles.points` select and a prop pass to `LessonView`. |
| 13 | `/profile` has no `LangToggle` of its own | Low | `app/profile/page.tsx`, `features/profile/components/ProfileView.tsx` | Now that `useLang` persists the choice (localStorage), `WalletPanel` *does* pick up an "en" set elsewhere, so `coin_packages.name_en` is no longer unreachable — this entry was rewritten from its original "unreachable branch" framing, which is now inaccurate. What remains: `ProfileView` receives a hardcoded `lang="ar"` prop from the server page, so the panels driven by that prop (DNA, scores, skills, certificates, capabilities, recommendations) stay Arabic regardless of the persisted choice, and a user can't switch language *while on* `/profile` at all. The page is therefore internally inconsistent: `WalletPanel` follows the persisted language, everything around it does not. | Small — same fix pattern already applied to the lesson player: thin server page + a client view owning `useLang()`/`LangToggle`, passing `lang` down instead of a hardcoded literal. |
| 14 | Two `features/lms` components import `sendAgentMessage` from `features/agent`, violating the import-direction rule | Low | `features/lms/components/LanguageTaskCard.tsx`, `features/lms/components/PronunciationPractice.tsx` | CLAUDE.md #1 forbids a feature importing from a sibling feature — shared composition is supposed to happen in `app/` pages. `LanguageTaskCard` introduced this first; `PronunciationPractice` followed the existing precedent rather than inventing a second, inconsistent pattern. Both work correctly today, so this is structural drift rather than a live defect: the rule exists to stop `features/` becoming an implicit dependency graph, and each new sibling import makes the eventual untangling larger. | Small — move `sendAgentMessage` (and its `AgentMsg` type) to `shared/services/` or `shared/lib/`, since it is just a typed `fetch` to `/api/agent` with no agent-feature-specific logic, then update both consumers. Deliberately out of scope for the batch that surfaced it. |
| 15 | ~~Abandoned placement conversations are an unbounded real OpenAI cost~~ **CLOSED (migration 029)** | ~~High~~ resolved | `supabase/migrations/029_placement_usage_quota.sql`, `app/api/agent/placement/route.ts` | `placement_usage` now holds a durable per-user lifetime counter, capped at 40 messages across every session and every restart. `consume_placement_quota()` checks and increments in ONE atomic statement (a separate read-then-write would let two concurrent requests both see 39 and both proceed), and the cap is a constant *inside* the function rather than a parameter — PostgREST exposes every public function, so a `p_cap` argument would have been trivially raised by the caller. The table has a read-only owner policy and **no write policy**, so nobody can reset their own cost cap. The in-memory `rateLimit()` remains but now only shapes bursts. Verified live: one real message incremented the counter to 1 and returned a genuine reply (no regression); calls 38-40 returned `allowed:true` and call 41 `allowed:false` with the stored count pinned at **40, not 41**; a real route request then returned **429** logging `placement_quota_exhausted` with **no** `placement_reply_sent` — and, warm, in a flat **~1.4s** across three consecutive calls, which is the Supabase round-trip alone and far below real gpt-4o latency, so the absence of a model call is shown positively rather than inferred from a missing log line. After a **full server restart** the counter still read 40 and the route still refused — the exact scenario that reset the old in-memory limiter. | Done. |
| 16 | The floating agent is mounted page-by-page instead of once in a route group | Low (rises with each new page) | `app/dashboard/page.tsx`, `app/profile/page.tsx`, `app/courses/page.tsx`, `app/courses/[id]/lessons/[lessonId]/page.tsx` | Deliberate owner decision when the floating agent shipped: compose per page (the existing `placementSlot`/`assistantSlot` pattern) rather than reorganize `app/` mid-feature. It is correct today and has a real safety benefit — each host page already resolves the user server-side, so a signed-out visitor never receives the component at all. The cost is that every future page that should show the agent must remember to mount it, and the four current call sites duplicate the same `getAgentInitialState` + conditional render. | Small-Medium — a route group (`app/(app)/layout.tsx`) wrapping the signed-in pages, resolving the user and mounting `FloatingAgent` once. Worth doing when the host-page count grows past a handful, not before; it changes the shape of `app/`, so it deserves its own decision rather than riding along with a feature. |
| 17 | ~~`ai_conversations` is written to but never read back — the agent forgets every conversation on reload~~ **CLOSED (migration 033)** | ~~Medium-High~~ resolved | `supabase/migrations/033_agent_messages_read_back.sql`, `app/api/agent/route.ts`, `features/agent/services/agent.client.ts`, `features/agent/components/AgentChat.tsx`, `features/agent/components/FloatingAgent.tsx` | `ai_conversations` renamed to `agent_messages` (reused, not duplicated — it already had exactly the right shape and an index built for this exact query) and locked down: no client-writable path at all (030's `forbid_client_write` STATEMENT trigger, reused unchanged), `record_agent_turn()` (security definer) the only writer, called once after a real OpenAI reply exists rather than the old two inserts split across the call. Both the OpenAI context and each UI surface's own "restore on load" read use the same `AGENT_CONTEXT_WINDOW` (20, `shared/constants/agent.ts`) most recent rows — an explicit, documented cost/speed tradeoff (memory beyond roughly the last ~10 exchanges is deliberately sacrificed), not an oversight. The client-supplied `history` field is gone entirely (schema, client helper, all 4 call sites) — the server derives its own context, never trusts a client-sent transcript. Both chat surfaces (`FloatingAgent`, `AgentChat`) now seed from the same persisted history — each still keeps its own read, so the "one continuous thread across surfaces" product question stays open (not part of this fix), but neither surface goes blank on reload/navigation anymore. | Done. |
| 18 | A mid-page language switch does not reach the floating agent until reload | Low | `shared/hooks/useLang.ts`, `features/agent/components/FloatingAgent.tsx`, `features/lms/components/LessonView.tsx` | `useLang` persists to localStorage but each component instance reads it once, in a mount-time `useEffect`, and there is no subscription between instances. Verified live: with the lesson player switched to EN (`wow.lang = "en"`, lesson labels reading "Listen"/"Pronounce"), the floating agent's own labels stayed Arabic; after a reload it correctly showed "Open your agent". So the language is right on every fresh page load and only lags a switch made while the page is open. Same root cause as #13 — `useLang` is per-instance state pretending to be app-wide state. | Small-Medium — one shared source of truth (a `LangProvider` context in a client wrapper, or a `storage`/custom-event subscription inside `useLang`) so every instance re-renders on a change. Fixing it once resolves #13 and #18 together. |
| 19 | ~~The lesson-context truncation path has never run on real data~~ **CLOSED (2026-08-15 status audit, by measurement)** | ~~Low~~ resolved | `features/lms/services/lesson.service.ts` (`getLessonAgentContext`, `clip`) | The caps that keep the agent's per-message lesson block bounded (1200 chars per AR/EN body, 600 for the grammar explanation, 25 vocabulary pairs) were **not exercised by any lesson that existed when this was written**. Measured against all 19 real lessons in the DB at the time: longest AR body 415, longest EN body 557, longest grammar explanation 317, largest vocabulary list 5 — every value far below its cap. **Closure evidence**: the 2026-08-15 status audit re-measured against all 37 real lessons now in the DB (Level 2 and Level 3 content authored since) — `select max(length(translations->'ar'->>'body')), max(length(translations->'en'->>'body')), max(length(content->'grammar_point'->>'explanation_ar'))` returned **2624 / 3201 / 687**, all three past their caps (1200 / 1200 / 600), confirming `clip()` genuinely fires on real production content on every agent context build for those lessons. One remaining nuance, stated precisely rather than overclaimed: this is measured from stored content length, not from tracing one live `/api/agent` request through `clip()` itself and inspecting its `truncated` flag on the wire — the underlying mechanism (`MAX_BODY_CHARS`/`MAX_GRAMMAR_CHARS` constants, unchanged since this item was written) is verified by code reading to apply unconditionally to any body over the cap, so the inference is direct, but a single traced live call was not additionally performed. | Done. |
| 20 | ~~`profiles.points`/`level` self-awardable via PostgREST~~ **CLOSED (migration 027)** | ~~critical~~ resolved | `supabase/migrations/027_points_award_hardening.sql` | Points/level are no longer writable from any client session — a direct `PATCH` is refused with `42501`. Payouts go through two security-definer functions that each verify one real event and refuse to pay twice for it: `award_lesson_points(lesson_id)` checks the `lesson_progress` row, `award_quiz_points(attempt_id)` (013, extended not forked) checks a real passed attempt on either the assessor or auto branch. Deliberately NOT a generic `award_points(reason)`: that would have been a worse hole than the one being closed — repeatable minting of `PMP_LEVEL_COMPLETE` instead of a one-time set. Verified live: a real lesson completion moved points 10 → 20; four direct PATCH shapes were refused 403/42501 with values unchanged on independent read; replaying the RPC on an already-paid lesson returned `false` with no change; the assessor path still paid out (a second account 0 → 20 on a genuinely passed attempt). | Done. |
| 21 | ~~`roles.assign` has no write path at all~~ **CLOSED (migration 031)** | ~~Medium~~ resolved | `supabase/migrations/031_assign_role.sql`, `app/admin/roles/`, `app/api/admin/roles/route.ts` | `assign_role(p_user, p_role)` (security definer, same shape as `update_pricing_unit`) verifies `has_permission('roles.assign')`, plus `has_permission('roles.assign_super')` for a super_admin target — belt-and-suspenders with the pre-existing `trg_guard_super_admin` (003), which still fires on the UPDATE itself regardless of caller. A new profiles SELECT policy, scoped to the same `roles.assign` permission, lets the screen list users to assign roles to; it ORs with the existing owner-only policy rather than replacing it. Reused the existing `roles.assign`/`roles.assign_super` permissions — no new permission introduced. Verified live: a real `admin` account promoted another account to `moderator` (persisted, `audit_log` row written); the same account attempting to grant `super_admin` was refused **403/42501** (`"Only a super_admin can assign super_admin"`), target role unchanged; an unprivileged account attempting to self-promote to `admin` via the same RPC was refused **403/42501** (`"Not authorized to assign roles"`); the new read policy correctly narrowed back to owner-only the moment the test account's own role was reverted to `user`. | Done. |
| 22 | `lessons.content->...->coin_cost` is dead data still present in every lesson row | Low | `lessons.content` (009, 023), `features/lms/services/lesson.service.ts` | Since 024 the coin cost is read from `pricing_units` by action type; the numbers still sitting in the lesson jsonb decide nothing — not display, not charging — and `resolveLanguageTask` no longer reads them. They were left in place deliberately (no destructive migration over authored content, and historical `language_task_submissions` rows keep their own snapshot regardless). The risk is purely that a future reader trusts them: they will silently disagree with reality the first time an admin changes a price. | Small — a cleanup migration stripping the `coin_cost` keys once the pricing table has been live long enough to be trusted. Explicitly NOT now, by owner decision. |
| 23 | `shared/lib/logger.ts` still says `audit_log` is a TODO, and it is still writing to only a handful of tables | Low | `shared/lib/logger.ts`, `audit_log` (003) | 024 made the pricing functions `audit_log`'s first real writers, so the table is no longer dead — but the stdout-only `auditLog()` helper in `logger.ts` still carries a comment saying a real trail "should" exist. **Correction (2026-08-15 status audit, by grep)**: this item's own text undercounted the writers even at the time — `grep "insert into public.audit_log"` across every migration finds **4 real call sites, not 1**: `024` (×2, pricing), `031` (`assign_role`), `034` (`grant_capability`) — the latter two are each independently confirmed by their own now-closed TECH_DEBT entries (#21, #26), just never cross-referenced back into this row. The core claim stands and is still true: role grants/pricing/capability grants audit; content-review decisions (015b/c), wallet credits, and most other staff actions still do not, so RBAC.md's "*every* staff action writes to `audit_log`" rule remains aspirational, just less far from true than this row implied. | Small per call site — have `auditLog()` insert through a security-definer function (the 024 functions show the shape) and add calls at the remaining staff-action sites, starting with the curriculum-review decisions.
| 24 | ~~A write refused by "no RLS policy" returns **200/204 with zero rows** instead of an explicit refusal~~ **CLOSED (migration 030)** | ~~Low~~ resolved | `supabase/migrations/030_forbid_client_writes_pricing_placement.sql` | A row-level (`FOR EACH ROW`) trigger — the 026 pattern — does **not** work for this case: with no write policy at all, RLS filters every row to zero *during the scan*, before any row-level trigger is ever invoked, so the trigger would never fire. The fix is a **`FOR EACH STATEMENT`** trigger, which fires once per statement unconditionally, before RLS row-filtering happens. `forbid_client_write()` raises `42501` for `current_user in ('authenticated','anon')` and is deliberately SECURITY INVOKER (not DEFINER — 026's own lesson), so `update_pricing_unit`/`consume_placement_quota` (both SECURITY DEFINER, running as the function owner) pass through untouched. Verified live: the exact original attack replayed — `PATCH pricing_units`, `PATCH`/`DELETE`/`INSERT placement_usage` from an unprivileged session — now all return **403** with a named reason (`"Direct UPDATE on pricing_units is not permitted — this table is only written by a verified server-side function"`), and an independent read confirmed the data never changed. No regression: `consume_placement_quota()` still incremented a real counter 0→1, and `update_pricing_unit()` still moved a real price 3→99→3, both from genuinely privileged sessions. | Done. |
| 25 | ~~The full quiz answer key is readable by any enrolled student over PostgREST~~ **CLOSED (migration 028)** | ~~critical~~ resolved | `supabase/migrations/028_quiz_answer_key_isolation.sql` | The key moved to `quiz_answer_keys` (RLS on, **zero policies** — no client role can read it) and was **physically removed** from the `question` jsonb, so "correct_index is never sent to the client" is now a property of the data, not of a route that could be bypassed. Scoring moved into `submit_quiz_attempt()`, a security-definer function that compares internally and returns only an aggregate score — no per-question breakdown, so the key cannot be inferred by diffing attempts. Enrollment and the one-attempt rule moved into the same transaction as the insert, closing a race the route had. Verified live: a direct read of `quiz_answer_keys` returns `[]`; all 18 question objects now contain exactly `options,text`; the embedded-join bypass (`select=id,quiz_answer_keys(correct_index)`) returns `null`; correct answers through the real route still score **100** and wrong answers score **0.00**; the hybrid assessor path still paid a fresh account **0 → 20** on the approved 100% attempt while the unapproved 0% attempt stayed at 0. | Done. |
| 26 | ~~`instructor`/`mentor`/`assessor` were self-grantable with zero verification~~ **CLOSED (migration 034)** | ~~critical~~ resolved | `supabase/migrations/034_grant_capability.sql`, `app/admin/roles/`, `app/api/admin/capabilities/route.ts`, `features/profile/components/ActivateCapabilityButton.tsx` | Found while live-testing migration 032: the same broad "Own capabilities: manage" policy meant for `learner`/`job_seeker`/`freelancer`/`client` let any account self-grant `assessor` (or `instructor`/`mentor`) with one PostgREST call and immediately gain real authority to grade other users' quizzes. Confirmed before writing any code that no legitimate alternative existed — `earner_profiles.verification_status`/`assessor_calibration_results` (008) are pure dead schema, read/written by nothing in `app/` or `features/`; every real authority check in this codebase only ever checks `user_capabilities` membership. `grant_capability(p_user, p_capability)` (security definer, same shape as `assign_role`/031) checks `has_permission('users.manage')` — confirmed against `role_permissions` before choosing it, not assumed — and is the only path to the three privileged capabilities; the four self-service ones are unaffected. `/admin/roles` gained a second, independently-gated section rather than a new page. Verified live: self-grant of all three privileged capabilities refused **403/42501** for a plain account, with an independent read confirming none landed; the four self-service capabilities still granted identically to before; a real `admin` account granted `assessor` to another account via `grant_capability`, landed on the target's own independent read, and wrote a real `audit_log` row; and — negative control — the same admin session was still refused attempting the identical grant as a **direct** table write, proving the function is the only door even for a caller who already holds the permission. | Done. |
| 27 | ~~8 of 17 pages still hardcode `const lang = "ar" as const`~~ **CLOSED for 3, reduced scope for 5** | ~~Medium~~ Low | `app/admin/pricing/page.tsx`, `app/admin/roles/page.tsx`, `app/join/[code]/page.tsx` (full), `features/admin/components/AdminPricingPageContent.tsx`, `features/admin/components/AdminRolesPageContent.tsx`; `app/assessor/queue/page.tsx`, `app/instructor/courses/page.tsx`, `app/instructor/courses/[id]/page.tsx`, `app/instructor/courses/[id]/sessions/page.tsx`, `app/instructor/review/page.tsx` (light), `features/lms/components/AssessorQueuePageContent.tsx`, `features/instructor/components/Instructor*PageContent.tsx` | Split by owner decision into two depths. **Full** (`/admin/pricing`, `/admin/roles`): their client views already took `lang` as a plain prop (no independent `useLang()`), so these got the exact five-page shape — `getServerLang()` + one `useLang(initialLang)`+`LangToggle` owner passing `lang` straight through. `/join/[code]` is a one-line `getServerLang()` swap (pure redirect page, no persistent content to toggle). **Light** (the remaining 5): their sub-components (`CreateCourseForm`, `CurriculumContributionSection`, `CourseContentManager` — 3 independent instances in one file — `InviteLinkCard`, `SessionsManager`, `ReviewQueue`, `AssessorQueue`) each already ran their own independent `useLang("ar")`, unlike the five originally-migrated pages (which had been consolidated to one `useLang()` per page as part of that same migration). Converting all of them to prop-driven `lang` was judged bigger than "small per page" once actually surveyed — deferred by owner decision. What shipped instead: only the page shell (Logo/title/breadcrumbs/`LangToggle`) reads `getServerLang()`/reacts live; each sub-component keeps self-correcting to the cookie on its own mount, same accepted flash-then-correct gap as #18, not a new one. | Remaining: converting the light-group components to prop-driven `lang` (removing their independent `useLang("ar")` calls) so a toggle click reaches them instantly instead of on next mount — same shape as #18's own fix, whenever that's picked up. **Correction (2026-08-15 status audit, by grep)**: `grep -rln 'useLang("ar")' app/ features/` finds **21 files still calling it independently, not the ~7 named above** — the extra 14 include files never named in this row at all (`features/agent/components/AgentNamePicker.tsx`, `features/auth/components/LoginForm.tsx`/`SignUpForm.tsx`, `features/landing/components/LandingPage.tsx`, `features/lms/components/EnrollButton.tsx`/`LiveSessionsUpcoming.tsx`, `features/onboarding/components/OnboardingWizard.tsx`, and several more under `features/instructor/`). The "light group" scope this row describes was never meant to be exhaustive of every independent `useLang("ar")` in the codebase, only the batch surveyed in that round — this note exists so the next person picking up #18/#27 sizes the real remaining work correctly instead of assuming ~7 files. |
| 28 | ~~`/courses` course cards render title/summary in whatever language they were authored in~~ **CLOSED (migration 035_courses_translations)** | ~~Low~~ resolved | `supabase/migrations/035_courses_translations.sql`, `features/lms/services/course.service.ts` (`resolveCourseText`), `features/lms/components/CourseCatalog.tsx`, `features/lms/components/CourseDetailView.tsx` | Mirrors `lessons.translations` (008) exactly rather than inventing a new shape: `courses.translations jsonb not null default '{}'::jsonb`, `{ar: {title, summary}, en: {...}}`, with the existing `title`/`summary` columns kept as the canonical/fallback values — `resolveCourseText(course, lang)` picks `translations[lang]` and falls back to the canonical columns when a language is missing, same two-level fallback `LessonView` already used for `lesson.translations`. No RLS change needed: "Courses: user-owner manages"/"org staff manage" (004) are row-level, already covering the new column. Verified live: PATCHing `translations.en.title` on an owned course made `/courses/[id]` render that English title with the toggle on `en`, and correctly fell back to the canonical Arabic title on `ar` (translations.ar left empty) — both directions confirmed, not just the write. `description`/`language` (both dead — confirmed via live query: `description` is `null` on all 3 real rows and never selected by any query; `language` is selected but never read anywhere) were deliberately left in place, same call as #22's `coin_cost`. | Done for the schema/resolution mechanism. Still open: an actual translation-authoring pass to populate `translations.en` on real course content — this migration only adds the capability. |

| 29 | ~~A security header silently disabled a shipped feature for its entire life — `Permissions-Policy: microphone=()` denied the microphone to our own origin, so PronunciationPractice's recording half (021) could never obtain one~~ **CLOSED (owner-confirmed live device check, 2026-08-15)** | ~~Medium~~ resolved | `next.config.js`, `features/lms/components/PronunciationPractice.tsx` | Found while sizing the voice-call feature (Sprint 3.4 Task 3), not by any test. `microphone=()` is an **empty allowlist**, which per the Permissions-Policy spec denies the feature to *every* origin **including same-origin** — not "default", not "prompt". It shipped in Sprint 1.5's security-headers pass and predates 021 by several sprints. **Proven, not inferred**: the header was confirmed on a live response (`curl -I`), `document.permissionsPolicy.allowsFeature('microphone')` returned `false` on the running app, and `window.self === window.top` confirmed the page was a top-level frame, so that reading came from the HTTP header and not from an embedding frame. It flipped to `true` the moment the header changed, with `camera`/`geolocation` still denied. Header fixed to `microphone=(self)` in the same session this was found; `camera`/`geolocation` deliberately left fully denied since nothing in the codebase requests either. **Closure evidence**: the header fix was independently re-confirmed still present in `next.config.js` during the 2026-08-15 status audit, and the owner directly confirmed the outstanding live-device check ("did recording actually start working") during that same audit — this is the one item on this list whose final confirmation could only ever come from the owner's own browser/microphone, not from an automated session (the automated browser used throughout this project blocks device capture independently of page policy, which is exactly why this stayed open as long as it did). | Done. |

| 30 | The "name your agent" screen reappears on a different page after the agent has already been named — **the exact symptom the 4-item round claimed to close, reported again by the owner** | Medium (user-facing, and it contradicts a closed item) | `features/agent/components/FloatingAgent.tsx`, `features/agent/components/AgentChat.tsx`, `features/agent/services/agent.client.ts` (`getFreshAgentState`), `features/agent/services/agent.service.ts` (`getAgentInitialState`) | Owner-reported during the voice-call round's live testing: named the agent on `/dashboard`, then opened the floating agent from a lesson page and was asked to name it from scratch again. This is precisely what `getFreshAgentState()` was added to prevent — it re-reads `user_agent_profiles` on every mount and corrects `needsNaming` **true → false** (one-directional, so an open conversation can never be yanked back to the naming screen). **What has been verified since the report**: the `trg_agent_profiles_updated_at` trigger genuinely fires — a real rename moved `updated_at` to `23:40:48` against a `created_at` of `23:28:17` on a live row — so the `updated_at === created_at` formula both `getAgentInitialState` (server) and `getFreshAgentState` (client) rely on has sound input. **REPRODUCTION ATTEMPTED AND FAILED (2026-08-02)** — recorded in full because a failed reproduction is evidence, not a dead end. Four fresh accounts, complete signup → onboarding → name → navigate flows, run against **both** `npm run dev` **and a real `npm run build && npm start` production server** (the Router Cache behaves differently in dev, so testing only there would have proved nothing). Two orderings were tried: (a) name on `/dashboard`, then first-ever visit to a lesson page; (b) the harder one — visit the lesson page **while still unnamed** so its RSC payload is cached with `needsNaming: true`, then name on `/dashboard`, then soft-navigate back to that cached lesson page. **In every run the correct chosen name appeared and the naming screen did not return**, sampled at 120ms after opening the panel as well as after 3s, so it was not merely a fast self-correction masking the fault. **The silent-zero-row theory is disproved**: the naming UPDATE was verified to land on three separate accounts (`updated_at` genuinely moved away from `created_at` each time). **What remains unknown** is the trigger condition. The leading hypothesis had been that the owner's session was running code that predates `getFreshAgentState` — **eliminated**: the owner confirmed via `git log --oneline -1` in the exact directory the affected server ran from that it was on `4c9a656`, *after* this very entry's own logging commit, i.e. on top of every fix discussed above. It was not an old build. **SECOND REPRODUCTION ATTEMPT, same day**, targeting the one procedural difference the first attempt hadn't covered: the owner named the agent from **`/profile`**, not `/dashboard` — every prior run had named on `/dashboard`. Retested on a fifth fresh account, covering both navigation types from `/profile`: (c) name on `/profile` → hard navigation (typed URL) to a lesson page; (d) name on `/profile` → enroll → **soft navigation** via real in-app link clicks (catalog → course → a lesson never before visited that session) to a lesson page. **Both reproduced the correct name with no naming screen**, sampled at 120ms and after 2.6s; the database confirmed the rename landed. This closes out `/profile` as a distinguishing variable, the same way `/dashboard` was already closed out. **Status after five accounts covering every combination of {naming surface: dashboard, profile} × {navigation: hard, soft} × {cache ordering: visit-before-name, visit-after-name} × {environment: dev, production build}**: none reproduce. The mechanism as coded is verified correct in every scenario constructible from the report so far. What has NOT been tried, because it cannot be from a fresh automated session: the owner's own browser tab carried forward from earlier that day, rather than a clean load. | Not blocked on effort; blocked on a variable only the owner's own browser can supply. If this recurs, the highest-value evidence is not a sixth reproduction attempt but real telemetry captured live at the moment it happens — the Network tab (was the `user_agent_profiles` fetch made, and what did it return?), the Console, and whether a hard reload (Ctrl+Shift+R) clears it, a question asked in the first pass and still unanswered. `setAgentChosenName` now `.select()`s (shipped separately) and stands regardless of root cause. |
| 31 | `/profile` briefly shows a wallet balance of 0 while the real balance is correct | Low (display only — the ledger was never wrong) | `features/profile/components/WalletPanel.tsx`, `app/profile/page.tsx` | Owner-reported in the same session, after a real purchase and after voice-call activity: the panel showed **0 coins** while the database held 19 throughout. Confirmed by the owner via direct query that this is **presentation only, not a balance or security defect** — across three voice-call cycles plus a pronunciation charge plus the welcome grant, `wallets.balance` equalled `sum(coin_transactions.amount)` **exactly**, with no drift. So the money is right and only the number on screen is briefly wrong. Most likely the same Router Cache staleness class already fixed twice in this codebase for the agent's `chosen_name` (a server-rendered prop rendering before a fresh client read lands), and `0` specifically suggests a default-initialised state rendering before the real value arrives rather than a wrong value being fetched. Worth more than its severity suggests because the coin balance is the single number users watch hardest, and showing them zero right after they paid is alarming in a way a stale course title is not. | Small — either seed the panel from the server-resolved balance instead of a `0` default, or apply the same mount-time direct-read pattern `getFreshAgentState` uses. Verify with a real purchase, not a mocked one. |

| 32 | ~~`public.badges` was silently unreadable over PostgREST to every client role since it was created~~ **CLOSED (migration 039)** | ~~Medium~~ resolved | `supabase/migrations/039_badges_read_policy.sql` | Found while live-testing 038's games: real rows existed (038's own self-check confirmed it server-side) but a signed-in user's REST read returned `[]` — confirmed by contrast against `pricing_units`, which the same JWT read fine. No migration file had ever touched `badges`' RLS, so the likely cause was RLS toggled on directly from the Supabase dashboard's own security advisor with no policy ever added — the same "RLS enabled, zero policies" signature this codebase uses on purpose elsewhere (`quiz_answer_keys`, `game_spotter_answer_keys`), just accidental here. Net effect: `user_badges`' embedded join to `badges` always resolved `name`/`description`/`icon` to `null`. **Verified live end to end**: a fresh signed-in account now reads all 9 `badges` rows (the 4 original + 038's 5); an unauthenticated request is still correctly refused (empty); and a real account that played and completed Charter Builder (project variant) now gets a correctly-resolved embedded join — `{name:"Charter Master", icon:"📜", description:"..."}` — not `null`. | Done. |
| 33 | ~~`public.system_actors` unreadable over PostgREST to every client role, same "RLS silently enabled, zero policies" signature as #32~~ **CLOSED (migrations 044 + 045)** | ~~High~~ resolved | `supabase/migrations/044_reseed_system_actors.sql`, `045_system_actors_read_policy.sql`, `features/lms/services/dna.service.ts` (`recomputeEmployabilityScore`), `app/api/agent/route.ts` (recommendation write) | Found while live-testing migration 043's `career_scores.score_type` FK — needed a real `system_actors` row, and a signed-in JWT read returned `content-range: */0` (contrasted directly against `career_score_types`' own read in the same request returning `0-2/3`). First hypothesis was "003's seed never landed," so 044 re-seeded (`on conflict do nothing`) — **044's own self-check passed (runs as table owner, bypasses RLS) but a real client JWT still read `[]` afterward**, the exact signature #32 already diagnosed once for `badges`: RLS silently toggled on (most likely via the Supabase dashboard's own security advisor) with zero policies ever added, invisible to any migration diff since no tracked migration touched it. 045 is the actual fix: `enable row level security` + `for select using (true)` (matches 013's original `anon + authenticated` grant scope, since `system_actors` is public reference data, not owner-scoped like most RLS here). **Impact while broken, both silent no-ops with no error/log anywhere — exactly the failure T6 warns against**: `if (novaActor) {...}` in `app/api/agent/route.ts` silently dropped every AI-generated `career_recommendations` row; `if (!novaActor) return;` in `recomputeEmployabilityScore` silently skipped every Employability recomputation. **Verified live after 045**: a real signed-in JWT now resolves `nova` via the exact query both code paths use; a `career_scores` insert with a fabricated `score_type` is rejected with `23503` against `career_scores_score_type_fkey` specifically (not a coincidental NOT NULL failure); a real insert with a genuine seeded type + the resolved `nova` id succeeds with a full `201` row. | Done. |
| 34 | `content_drafts` (062) can accumulate multiple stacked, unpublished draft rows for the same target if "Save draft" is clicked more than once before publishing | Low (no security or correctness impact — cosmetic/cleanup only) | `supabase/migrations/062_content_drafts.sql`, `features/admin/components/KbScenarioEditor.tsx`, `KbScoringRuleEditor.tsx`, `BadgeEditor.tsx` | Deliberate v1 simplification, not an oversight: every "Save draft" click inserts a NEW `content_drafts` row rather than updating an existing pending one for the same `target_id`. The admin UI always shows/acts on only the most recent pending draft per target (`created_at` max), so this never causes a wrong publish or a security gap — RLS still gates every row identically, and `publish_content_draft()` only ever touches the one draft id it's given. The only real effect is unpublished, superseded draft rows piling up in the table over time with no cleanup path. | Not urgent. If it becomes noticeable, either (a) have the save action `UPDATE` the existing pending draft instead of inserting (RLS's "content-manage edits own draft" policy already allows this for the creator), or (b) add a periodic cleanup marking superseded drafts `discarded`. |

## تحذير: محادثات تحديد المستوى المهجورة (migration 022, 2026-07-25)

محادثة تحديد المستوى مجانية وغير مكررة **بعد اكتمالها فقط** — حارس المرة الواحدة (المفتاح الأساسي في `user_language_profiles`) لا يمنع من يبدأ المحادثة ويهجرها قبل إنتاج الوكيل لكتلة التقييم من إعادة البدء بلا حد، وكل رسالة استدعاء OpenAI حقيقي بتكلفة حقيقية وبلا أي مقابل كوينز. الحارس الوحيد حالياً محدد معدل في الذاكرة (15 رسالة / 10 دقائق) **يسقط كلياً عند إعادة تشغيل الخادم ويتضاعف بعدد الـinstances على serverless**. يجب حل حقيقي (عدّاد محاولات مخزَّن في قاعدة البيانات أو حد يومي دائم) **قبل أي حركة مرور Beta حقيقية تتجاوز دائرة الاختبار المغلقة** — نفس درجة إلزامية تحذير الشراء المحاكى أعلاه.

## ~~تحذير: الشراء المحاكى بلا حد~~ — مُغلَق (2026-07-29)

المالك اتخذ قراراً واعياً ومؤجَّلاً عمداً: تفعيل شراء كوينز **محاكى محلياً بالكامل** (`credit_coins()`، مسار `/api/wallet/purchase`) لأغراض الاختبار، بلا بوابة دفع حقيقية وبلا أي حد لتكرار الشراء. تحقّق حي أثناء الاختبار: نفس الحساب اشترى نفس الباقة ثلاث مرات متتالية بنجاح (25 → 325 → 625 → 925 كوينز)، كل عملية سجّلت صفاً حقيقياً في `coin_transactions`.

**أُغلق باختيار «تعطيل المسار كلياً» من الخيارات الثلاثة التي كان هذا البند يطرحها.** `/api/wallet/purchase` صار **مُعطَّلاً افتراضياً** ولا يعمل إلا إذا كان `WALLET_SIMULATION_ENABLED === "true"` صراحةً. متغير مفقود أو مكتوب خطأً **يفشل مغلقاً** — الاتجاه الصحيح لشيء يوزّع عملة مجاناً. الفحص أول سطر في المسار، **قبل التحقق من الجلسة**، لأن «هل الميزة موجودة أصلاً» ليس سؤالاً خاصاً بمستخدم؛ والرد **503 لا 403** لأن القدرة غائبة والمستخدم لم يخطئ. **تحقق حي للفرعين**: بلا المتغير → **503** برسالة عربية واضحة، والرصيد بقي **10** بلا أي صف جديد في `coin_transactions` (لا سكّ عملة)؛ ومع `WALLET_SIMULATION_ENABLED=true` → **200**، الرصيد `10 → 310` وصف حقيقي `+300 simulated_purchase` — لا انحدار في بيئة التطوير. الواجهة تعرض إشعاراً واضحاً وتعطّل أزرار الشراء عند التعطيل، **لكن الحماية الحقيقية في المسار** والواجهة عرض فقط. **ما لم يُحَل**: هذه ليست بوابة دفع ولا تدّعي ذلك — الشراء الحقيقي عمل منفصل لاحق؛ وعند تفعيل المحاكاة محلياً ما زال لا يوجد حد على التكرار، وهو مقبول فقط لأنها لم تعد قابلة للتفعيل في الإنتاج.

| 35 | ~~`instructor_assignments.status` could be set to `'accepted'` by a direct client PATCH, bypassing `accept_instructor_assignment()` entirely — i.e. accepting a paid request without paying~~ **CLOSED (migration 075)** | ~~critical~~ resolved | `supabase/migrations/075_instructor_accept_bypass_fix.sql`, `040_instructor_assignments.sql` (origin), `074_instructor_delivery.sql` | A seam between two migrations, not a single mistake. 040 shipped the UPDATE policy `with check (status = any (array['accepted','declined']))` in a deliberately schema-only round — correct at the time, since no payment path existed and `accepted` moved no money; 040's own header even recorded the follow-up obligation ("no forbid_client_write trigger is needed **yet** … added in the follow-up migration alongside the accept-and-charge function"). 074 then added `accept_instructor_assignment()` (which charges the learner) but never narrowed that policy, leaving two routes to the same end state: the function (charges) and a plain PostgREST PATCH (charges nothing, writes no `coin_transactions` row). **Why it survived undetected:** no UI calls any of 074's three functions — `git grep accept_instructor_assignment -- "*.ts" "*.tsx"` returns zero, same for `send_instructor_message`/`rate_instructor` — so neither the correct path nor the bypass was ever exercised by a user. The absent UI is precisely what hid it; it was found by reading 074 while preparing to build that UI. **Fix:** WITH CHECK narrowed to `status = 'declined'` only (declining moves no money, so it stays a plain client write), making the SECURITY DEFINER function the only door to `'accepted'` — same shape as 027/028/030/033. 074 untouched. **Verified by replaying the original attack, two real separate accounts, one variable changed:** before 075 the direct PATCH returned **200** with an independent read confirming `status=accepted`, learner balance **30 → 30 unchanged**, zero `instructor_request` transactions (damage proven, not just the write); after 075 the identical PATCH returned **403/42501** with `status` still `pending` and the balance untouched. Regressions re-run: direct decline still works (200, read back as `declined`), the RPC still charges correctly (**20 → 10** plus a `coin_transactions` row with a matching `ref_id`), and a third account is still refused **403/42501** by the RPC and gets a zero-row 200 on a direct PATCH. **Open follow-up recorded, not silently resolved:** `accept_instructor_assignment()` only permits the instructor to accept, so instructor-initiated invites (`initiated_by='instructor'`) now have no accept path at all — decline-only until the owner decides between building `learner_accept_instructor_invite()` (caller == payer, so `spend_coins()` applies directly) or dropping that direction. Not a regression from 075: the only prior way a learner could accept was the bypass itself. | Done. |

| 36 | `Leaked Password Protection` معطَّلة — **محجوبة بقيد خطة، ليست إهمالًا** | Low (محجوب) | إعدادات Supabase Auth (لا كود) | مستشار الأمان في Supabase يرفع هذا التحذير باستمرار، **وسيبقى مرفوعًا** — الميزة (فحص كلمة المرور مقابل قاعدة HaveIBeenPwned) **غير متاحة على الخطة المجانية**؛ توثيق Supabase صريح أنها Pro فأعلى. **مسجَّل هنا تحديدًا حتى لا تقترحه جلسة قادمة كأنه بند مُهمَل، ولا تكتب أن المستشار "نظيف"** — لن يكون نظيفًا ما دامت الخطة مجانية. القرار (الترقية أو القبول) قرار مالك تجاري، خارج النطاق التقني. | محجوب — لا عمل تقني ممكن قبل ترقية الخطة. |
| 37 | `Confirm email` مطفأ — أي شخص يسجّل بأي بريد دون إثبات ملكيته | Medium (قبل الإطلاق) | إعدادات Supabase Auth (لا كود) | شائع ومقبول أثناء التطوير (وهو ما جعل كل حسابات الاختبار في هذا المشروع ممكنة بلا صندوق بريد)، لكنه **قرار يجب أن يُتخذ قصدًا قبل أي إطلاق حقيقي** لا أن يُكتشف بعده: بلا تأكيد بريد يستطيع أي شخص التسجيل ببريد شخص آخر، وتصبح "ملكية البريد" غير مضمونة في أي مسار يعتمد عليها لاحقًا (استعادة كلمة مرور، إشعارات، ربط مؤسسي). **بند مفتوح لا حالة راهنة مقبولة.** | صغير تقنيًا (مفتاح واحد) — لكن تفعيله يتطلب أولًا التأكد من أن تدفق التسجيل يتعامل مع "حساب غير مؤكَّد" بوضوح، وأن حسابات الاختبار لن تُحجب. قرار توقيت للمالك. |
| 38 | لا يوجد أي مسار لتغيير كلمة المرور في المنتج كله، وتنبيه `weak_password` من الخادم يُهمَل صامتًا | Medium | `features/auth/`, `app/` — بالغياب لا بالوجود | **بحث مباشر (2026-08-16):** `updateUser` · `resetPasswordForEmail` · `reauthenticate` · `changePassword` — **صفر نتيجة** في `app/` و`features/` و`shared/`؛ ولا واجهة "نسيت كلمة المرور" مطلقًا. أي أن المستخدم **لا يستطيع تغيير كلمة مروره بأي طريقة**. ويتضاعف الأثر بعد تشديد سياسة كلمات المرور (2026-08-15): تسجيل الدخول بكلمة قديمة ضعيفة **ينجح** (HTTP 200 + جلسة كاملة) ويُرفق الخادم حقل `weak_password` يشرح سبب الضعف — لكن `supabase-js 2.110.7` لا يحوّله إلى خطأ (التحويل داخل `handleError` الذي لا يعمل إلا على استجابة غير 2xx)، فالتنبيه يُهمَل ولا يراه المستخدم. **فحصت هذا حيًا** بحساب اختبار ضُبطت تجزئة كلمة مروره على قيمة ضعيفة: الجلسة تُنشأ، `error === null`، والتوجيه يتم طبيعيًا. **الخبر الجيد: لا عطب في `LoginForm`** — النمط `if (signInError) { ...; return; }` لا يُفعَّل لأن الخطأ لا يصل أصلًا. **الخبر السيئ:** المستخدم لا يُنبَّه، **ولو نُبِّه لما استطاع فعل شيء** — لا مسار تغيير موجود. | متوسط — بناء تدفق "نسيت/غيّر كلمة المرور" (`resetPasswordForEmail` + صفحة تحديث)، ثم عرض تنبيه `weak_password` عند الدخول. ملاحظة تخطيطية: مفتاحا `Secure password change` وE`Require current password when updating` في Supabase **صفر تكلفة اليوم** (لا `updateUser()` قائم ليُكيَّف)، فالأوفر تفعيلهما **مع** بناء هذا التدفق لا قبله ولا بعده. |
| 39 | الأخضر الثلاثي (`tsc` + `lint` + `build`) لا يمسك كسر حدود Server/Client — والـCI لا يفتح أي صفحة | **High** (منهجي) | `.github/workflows/ci.yml`, كل `app/**/page.tsx` يمرّر `prop` إلى Client Component | **حدث مرّتين، لا مرّة — وهذا هو الدليل الحقيقي على أن الفجوة في الأدوات لا في الانتباه.** المرة الأولى: commit `245f4f3` — «`placementSlot` switched from a render-prop function (which Next.js rejects across the Server→Client boundary — **500'd /profile**)». المرة الثانية بعدها بأسابيع (2026-08-20، دفعة 078): مُرِّر `instructorQueueSlot` كدالة `(lang) => ReactNode` من Server Component إلى Client Component في `app/admin/roles/page.tsx` — **نفس الخطأ حرفيًا، في نفس الملف تقريبًا** (`245f4f3` عدّل `app/admin/roles/page.tsx` نفسه). `npx tsc --noEmit` **نجح**، `npm run lint` **نجح**، `npm run build` **نجح** — والصفحة تُرجع **500** وقت التشغيل: `"Functions cannot be passed directly to Client Components"`. القاعدة كانت **موثّقة أصلًا في المستودع** (`ProjectWorkspace.tsx:27`) ومكتوبة في رسالة التزام سابقة، ومع ذلك خولفت. لم يكشفه إلا تحميل الصفحة فعليًا في متصفّح — **في الحالتين**. **الفئة أوسع من هذه الحالة**: كل خطأ تسلسل عبر حدّ RSC (دوال، `Date`، `Map`/`Set`، `class`) غير مرئي للأدوات الثلاث. | **مُعالَج (2026-08-20) — بثلاثة إجراءات، لا بواحد.** (١) **القاعدة نُقلت إلى `CODING_GUIDELINES.md §5a`** بصيغة قاطعة مع الحادثتين كدليل. كانت موثّقة قبلًا في **تعليق داخل `ProjectWorkspace.tsx:27`** — ولم يمنع ذلك تكرارها، لأن تعليقًا داخل ملف واحد لا يقرؤه إلا من فتح ذلك الملف صدفةً. **موضع التوثيق كان المشكلة، لا غيابه.** (٢) **`npm run smoke` صار البوابة الرابعة** قبل كل التزام (`CLAUDE.md`)، يشغّل `next start` بجلسة حقيقية ويفشل على أي `>= 500` **أو** على `200` يعرض حدّ الخطأ العام. (٣) **`npm run smoke:public` في CI** — الشقّ العام وحده، لأن CI يبني بمفاتيح وهمية فلا جلسة فيه. **البوابة أُثبتت بإعادة إدخال العطل نفسه**: `tsc` نجح · `lint` نجح · `build` نجح · **`smoke` فشل بـexit 1 على `/admin/roles — HTTP 500`**، ثم أُعيد الملف. **الفجوة الباقية**: تغطية المسارات المحميّة داخل CI تحتاج مشروع Supabase اختباريًا — **مربوطة بـ#6، لا بندًا جديدًا**. و(ج) قاعدة ESLint **مؤجَّلة بقرار المالك**: تحتاج تتبّع الاستيرادات لمعرفة أي مكوّن `"use client"`، والإنذارات الكاذبة في قاعدة لينت تُدرِّب الجلسات على تجاهلها فتصير أسوأ من لا شيء. |
| 40 | `guard_instructor_profile_update()` (078) بلا `search_path` مثبَّت | Low | `supabase/migrations/078_*.sql` | الدوال الأخرى في نفس الترحيل (`submit_instructor_application`, `review_instructor_application`) مضبوطة بـ`set search_path = public`، وهذه ليست. **الأثر الفعلي منخفض جدًا وليس نظريًا فقط**: الدالة `SECURITY INVOKER` (عمدًا — انظر `SECURITY.md`)، فهي تعمل بصلاحية المستدعي لا المالك، **ولا تستدعي أي دالة ولا تقرأ أي جدول** — تقارن حقول `new`/`old` وترفع استثناءات فقط. فلا سطح لاختطاف `search_path` هنا، بخلاف `spend_coins` (007b) المسجَّلة أدناه والتي **تكتب** وهي `DEFINER`. مسجَّلة للاتساق لا للخطر. | صغير — `alter function public.guard_instructor_profile_update() set search_path = public;` في ترحيل لاحق. **لا يُعدَّل 078 نفسه** (قاعدة المشروع: لا تعديل على ترحيل مُطبَّق). |

| 41 | استطلاع (polling) لكل محادثة مفتوحة — مقايضة معلومة لا حلّ نهائي | Low (اليوم) → يرتفع مع الحمل | `features/instructors/components/AssignmentChat.tsx` | محادثة الأستاذ/الدارس تستطلع `instructor_messages` كل **10 ثوانٍ** ما دامت مفتوحة. **الاختيار مقصود ومبرَّر الآن**: بلا استطلاع لا يظهر ردّ الطرف الآخر إلا بإعادة تحميل يدوية، وهو في محادثة **مدفوعة** يُقرأ كعطل لا كتأخير. لكنه **يتراكم خطيًا مع عدد المحادثات المفتوحة**: 100 محادثة مفتوحة = 10 طلبات/ثانية على PostgREST بلا أي رسالة جديدة. **الوجهة Supabase Realtime** (اشتراك يدفع فيه الخادم بدل أن يسحب العميل)، ولم يُبنَ الآن لأنه **نمط جديد كليًا في هذا المشروع** ولا يستحق فتحه لأجل محادثة واحدة بحمل شبه معدوم. **قرار المالك: مقبول الآن، ويُعاد النظر حين يصير الحمل حقيقيًا — لا يُبنى استباقًا.** | متوسط — استبدال `setInterval` باشتراك Realtime على `instructor_messages` مُقيَّد بـ`assignment_id`. المؤشّر الذي يستدعي التنفيذ: عدد المحادثات النشطة المتزامنة، لا عدد المستخدمين. |

## غير مؤكَّد — لم يُتحقَّق منه (تدقيق 2026-08-15)

بنود ذُكرت أو طُلب فحصها أثناء تدقيق حالة 2026-08-15 (`WoW_Status_Audit_ClaudeCode_2026-08-15.docx`) لكن تعذّر التحقق منها فعليًا — مسجَّلة صراحةً هنا حتى لا تفترض جلسة قادمة أنها فُحصت لمجرد ذكرها في تقرير.

- ~~**قيمة `WALLET_SIMULATION_ENABLED` الفعلية في بيئة الإنتاج على Vercel**~~ — **تأكّدت (2026-08-16)، لم تعد بندًا غير مؤكَّد.** لوحة Vercel تحتوي ثلاثة متغيرات فقط ولا يوجد بينها `WALLET_SIMULATION_ENABLED` إطلاقًا. والأثر الفعلي أُثبت **حيًا على الإنتاج** لا استنتاجًا: `POST https://wo-w-sepia.vercel.app/api/wallet/purchase` بلا مصادقة رجّع **503** مع `{"simulationDisabled":true}` — وفحص العلَم في [`route.ts:32`](app/api/wallet/purchase/route.ts:32) يسبق قراءة الجلسة، فالرد 503 (لا 401) يثبت أن العلَم مطفأ فعلًا. **النتيجة الدقيقة: لا محاكاة ولا مسار دفع حقيقي — شراء الكوينز غير متاح في الإنتاج بتاتًا، وهو fail-closed مقصود.** (فحص مصاحب: الكود كله يستهلك أربعة متغيرات بيئة فقط، والثلاثة الأخرى مضبوطة — لا ميزة بُنيت بعد 21 يوليو تنتظر متغيرًا مفقودًا.)
- **موانع الإطلاق الخمسة** (سياسة القاصرين/موافقة ولي الأمر، شريك توريد عمالة مرخّص، استشارة قانونية محلية، بوابة دفع حقيقية، إخطار الضيوف) — قرارات عمل/قانونية بلا أثر كود يمكن فحصه مباشرة. حالتها كما تسجّلها `CLAUDE.md`/`RBAC.md` غير مؤكَّدة بفحص مستقل.
- **الاختبار الحي الكامل لمسار تسليم الأستاذ (migration 074)** — `accept_instructor_assignment()`/`instructor_messages`/`instructor_ratings` مبنية ومطبَّقة على الإنتاج (مؤكَّد عبر `pg_proc`/`information_schema.tables` مباشرة)، لكن سيناريوهات الاختبار الحي عبر الواجهة الفعلية (قبول→خصم فوري، رفض→صفر خصم، محادثة حية، هجوم حساب ثالث، تقييم بلا طلب مكتمل، رصيد غير كافٍ) طُلبت في نفس الجلسة وانقطع تنفيذها قبل وصول أي نتيجة. الكود موجود؛ التحقق الحي غير مكتمل.
- **الجداول الثلاثة `api_partners`، `horizon_episodes`، `pmp_levels`** — RLS مفعَّلة عليها بصفر سياسة (نفس توقيع #32/#33)، وتأكد أن لا كود تطبيقي حاليًا يقرأها (`grep` صفر نتيجة عبر `app/`/`features/`/`shared/`)، فهي غير مستغَلة الآن وغير ضارة عمليًا. لكن لم يُحسَم هل هذا ترك متعمَّد (جداول قديمة/غير مستخدَمة عمدًا) أم فجوة صامتة ثالثة من نفس فئة #32/#33 لم تُكتشَف بعد لأن لا كود يقرأها حاليًا ليكشفها.

## Already resolved this sprint (for the record, not open debt anymore)
- Missing `tsconfig.json` / `next.config.js` / `next-env.d.ts` / `.eslintrc.json` — project would not have built. **Fixed.**
- Client-controlled point amount (critical security hole). **Fixed.**
- No FK indexes on `enrollments`, `user_badges`, `ai_conversations`, `horizon_progress`. **Fixed (migration 002).**
- `profiles.updated_at` never actually updated. **Fixed (migration 002 trigger).**
- No `app/error.tsx` / `app/not-found.tsx`. **Fixed.**

## Already resolved in Sprint 3
- Nova's conversation `history` trusted from client with only a TS type
  — resolved back in Sprint 1.5 (`agentRequestSchema`/`novaRequestSchema`
  zod validation) but never removed from this table until now.
- Points-awarding had no caller that verified the underlying event
  actually happened — `app/api/lms/lessons/complete` and
  `app/api/lms/quizzes/submit`/`grade` now all verify server-side
  (RLS-gated reads / a real graded attempt) before calling
  `awardPoints`/`award_quiz_points`. **Fixed.**
- Five real RLS write-permission gaps found via two-real-account
  acceptance testing (`entity_skills`, `skill_evidence`,
  `quiz_attempts` UPDATE, `system_actors` grant, ambiguous `profiles`
  embed) — see migrations 010, 012, 013 and ROADMAP.md Sprint 3.
  **Fixed.**
