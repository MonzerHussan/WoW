# TECH_DEBT.md

| # | Item | Severity | Where | Why it matters | Fix effort |
|---|---|---|---|---|---|
| 1 | Bilingual `COPY` dictionary duplicated 3x | Medium | signup, login, onboarding pages | Every new UI string has to be added in 3 places by hand; they will drift. | Small — extract to `constants/i18n/*.ts`, shared per feature. |
| 2 | No shared UI primitives (`Button`, `Input`, `Select`) | Medium | every form page | Styling/behavior (disabled states, focus rings) is copy-pasted; a design tweak means editing N files. | Small-Medium — build `components/ui/{Button,Input}.tsx` once shadcn/ui is initialized, migrate forms one at a time. |
| 3 | No request validation library | Medium | all `app/api/*` routes | Validation is ad hoc per route; easy to miss a check on a new route. | Small — introduce zod, one shared `schemas/` folder. |
| 4 | No custom data hooks (`useUser`, `useProfile`) | Low-Medium | signup/login/dashboard | Supabase calls are inlined per-component instead of centralized, making it harder to test or swap data sources later. | Medium. |
| 5 | `profiles` conflates identity + gamification state | Low | `supabase/schema.sql` | Works fine now; will matter if gamification logic grows complex enough to want its own table/service boundary. | Defer — revisit only if Sprint 6 (Gamification) needs it. |
| 6 | Automated tests cover pure logic only — **not** RLS or security-definer behaviour | Reduced from High to Medium | `tests/`, `vitest.config.ts` | 48 unit tests now guard the parts that can be tested without a database: every zod schema (written around what it must **refuse**, including that injected `amount`/`coinCost`/`score` fields are stripped from the parsed output), `resolveLanguageTask` (which pricing key each task shape selects, and that the `coin_cost` buried in lesson content is ignored — the point of 024), `clip` (the truncation caps that TECH_DEBT #19 recorded as never exercised), `selectNewBadges`, and a test that reads `027_points_award_hardening.sql` and asserts its SQL constants still match `REASON_POINTS` — turning a "keep these in sync by hand" comment into a CI failure. **What is still uncovered is the category that produced every real vulnerability in this project**: RLS policies and security-definer functions. CI touches no live database, by design — there is no test Supabase project separate from production. See TESTING_POLICY.md for the explicit list of what is and isn't covered. | Remaining work: a dedicated test Supabase project with migrations applied automatically and policies exercised by fixture accounts. Until then those fixes stay under the "replay the original attack" rule, verified by hand. |
| 7 | ~~No CI~~ **CI is live** — but it validates code, not database behaviour | Reduced from Medium to Low | `.github/workflows/ci.yml` | Every push to `main` and every PR into it runs `npm ci` → `tsc --noEmit` → `npm run lint` → `npm run test` → `npm run build`, and a failure blocks the merge. `npm ci` (not `npm install`) so a mismatched lockfile fails loudly instead of CI passing on a different dependency tree than the one committed. Supabase/OpenAI env vars are obvious placeholders and `WALLET_SIMULATION_ENABLED` is pinned to `"false"`, matching production — nothing in CI reaches a live service. The `ci/**` branch trigger exists so the pipeline itself can be validated on a scratch branch, which is how it was proven to actually fail on a broken test rather than assumed to. | Remaining: nothing for code. Database-layer verification needs the test project described in #6. Optionally add a required-status-check branch protection rule on `main` so the gate cannot be bypassed by an admin merge. |
| 8 | Route-based folder structure will not scale past ~Sprint 3-4 | Medium | whole repo | Documented with a full migration plan in `PROJECT_STRUCTURE.md` — intentionally not executed yet. | Large — scheduled as first task of Sprint 2. |
| 9 | ~~`career_scores(employability)` is not recomputed when an assessor confirms a human/hybrid quiz attempt for someone else~~ **CLOSED (migration 032)** | ~~Medium~~ resolved | `supabase/migrations/032_employability_recompute_on_grade.sql`, `app/api/lms/quizzes/grade/route.ts` | `recompute_employability_score(p_attempt_id)` — security definer, same shape as `award_quiz_points` (027): re-verifies `graded_by = auth.uid()` and the caller holds the `assessor` capability (copied from `award_quiz_points`'s own branch A predicate, not shared via a helper — no two definer functions in this codebase share one). Running RLS unaffected, it reads `entity_skills`/`quiz_attempts`/`lesson_progress` directly as its owner — checked first that the assessor's own session could **not** do this (broad `lesson_progress` read has no assessor policy at all; `entity_skills` assessor read is scoped to `source='assessment'` only), so no new read policy was opened anywhere. Called synchronously from `grade/route.ts` right after `award_quiz_points` succeeds. Always inserts a **new** `career_scores` row — no history touched. The TS auto-path function (`dna.service.ts`) is untouched; this is a second, parallel path for the assessor caller, same relationship as `award_lesson_points`/`award_quiz_points`. | Done. |
| 10 | Third-party badge grants (an org/system awarding a badge the user didn't trigger themselves) have no write path | Low | `user_badges` | `013`'s "owner inserts own" policy only covers the self-serve case already used by `points.service.ts`. Nothing currently needs third-party grants, so this is a real but currently-unused gap. | Small — same `security definer` pattern, when a real caller (e.g. an org awarding a badge) exists. |
| 11 | `profiles.age` is a static integer captured once at onboarding, not `date_of_birth` | Low (for now) | `profiles` (migration 016), onboarding wizard, agent context | The number freezes at whatever the user typed and never advances — a 27-year-old today is still "27" in the DB a year from now, silently drifting from reality everywhere it's read (agent context, any future age-based logic). Accepted as-is for this stage since exact age collection is itself a brand-new, deliberately provisional decision (RBAC.md "تحديث حرج على سياسة القاصرين"). | Small — swap the column for `date_of_birth date` and compute age at query time; straightforward migration since only onboarding writes it today. |
| 12 | `profiles.points` balance isn't shown anywhere on the lesson page itself | Low | `features/lms/components/LessonView.tsx` | The dashboard shows points, but a learner completing a lesson (and earning `LESSON_COMPLETE` points) has no in-page confirmation of their running total — only the one-off "+N points earned" toast from `LessonCompleteButton`. | Small — `LessonPage` already fetches the user; one more `profiles.points` select and a prop pass to `LessonView`. |
| 13 | `/profile` has no `LangToggle` of its own | Low | `app/profile/page.tsx`, `features/profile/components/ProfileView.tsx` | Now that `useLang` persists the choice (localStorage), `WalletPanel` *does* pick up an "en" set elsewhere, so `coin_packages.name_en` is no longer unreachable — this entry was rewritten from its original "unreachable branch" framing, which is now inaccurate. What remains: `ProfileView` receives a hardcoded `lang="ar"` prop from the server page, so the panels driven by that prop (DNA, scores, skills, certificates, capabilities, recommendations) stay Arabic regardless of the persisted choice, and a user can't switch language *while on* `/profile` at all. The page is therefore internally inconsistent: `WalletPanel` follows the persisted language, everything around it does not. | Small — same fix pattern already applied to the lesson player: thin server page + a client view owning `useLang()`/`LangToggle`, passing `lang` down instead of a hardcoded literal. |
| 14 | Two `features/lms` components import `sendAgentMessage` from `features/agent`, violating the import-direction rule | Low | `features/lms/components/LanguageTaskCard.tsx`, `features/lms/components/PronunciationPractice.tsx` | CLAUDE.md #1 forbids a feature importing from a sibling feature — shared composition is supposed to happen in `app/` pages. `LanguageTaskCard` introduced this first; `PronunciationPractice` followed the existing precedent rather than inventing a second, inconsistent pattern. Both work correctly today, so this is structural drift rather than a live defect: the rule exists to stop `features/` becoming an implicit dependency graph, and each new sibling import makes the eventual untangling larger. | Small — move `sendAgentMessage` (and its `AgentMsg` type) to `shared/services/` or `shared/lib/`, since it is just a typed `fetch` to `/api/agent` with no agent-feature-specific logic, then update both consumers. Deliberately out of scope for the batch that surfaced it. |
| 15 | ~~Abandoned placement conversations are an unbounded real OpenAI cost~~ **CLOSED (migration 029)** | ~~High~~ resolved | `supabase/migrations/029_placement_usage_quota.sql`, `app/api/agent/placement/route.ts` | `placement_usage` now holds a durable per-user lifetime counter, capped at 40 messages across every session and every restart. `consume_placement_quota()` checks and increments in ONE atomic statement (a separate read-then-write would let two concurrent requests both see 39 and both proceed), and the cap is a constant *inside* the function rather than a parameter — PostgREST exposes every public function, so a `p_cap` argument would have been trivially raised by the caller. The table has a read-only owner policy and **no write policy**, so nobody can reset their own cost cap. The in-memory `rateLimit()` remains but now only shapes bursts. Verified live: one real message incremented the counter to 1 and returned a genuine reply (no regression); calls 38-40 returned `allowed:true` and call 41 `allowed:false` with the stored count pinned at **40, not 41**; a real route request then returned **429** logging `placement_quota_exhausted` with **no** `placement_reply_sent` — and, warm, in a flat **~1.4s** across three consecutive calls, which is the Supabase round-trip alone and far below real gpt-4o latency, so the absence of a model call is shown positively rather than inferred from a missing log line. After a **full server restart** the counter still read 40 and the route still refused — the exact scenario that reset the old in-memory limiter. | Done. |
| 16 | The floating agent is mounted page-by-page instead of once in a route group | Low (rises with each new page) | `app/dashboard/page.tsx`, `app/profile/page.tsx`, `app/courses/page.tsx`, `app/courses/[id]/lessons/[lessonId]/page.tsx` | Deliberate owner decision when the floating agent shipped: compose per page (the existing `placementSlot`/`assistantSlot` pattern) rather than reorganize `app/` mid-feature. It is correct today and has a real safety benefit — each host page already resolves the user server-side, so a signed-out visitor never receives the component at all. The cost is that every future page that should show the agent must remember to mount it, and the four current call sites duplicate the same `getAgentInitialState` + conditional render. | Small-Medium — a route group (`app/(app)/layout.tsx`) wrapping the signed-in pages, resolving the user and mounting `FloatingAgent` once. Worth doing when the host-page count grows past a handful, not before; it changes the shape of `app/`, so it deserves its own decision rather than riding along with a feature. |
| 17 | ~~`ai_conversations` is written to but never read back — the agent forgets every conversation on reload~~ **CLOSED (migration 033)** | ~~Medium-High~~ resolved | `supabase/migrations/033_agent_messages_read_back.sql`, `app/api/agent/route.ts`, `features/agent/services/agent.client.ts`, `features/agent/components/AgentChat.tsx`, `features/agent/components/FloatingAgent.tsx` | `ai_conversations` renamed to `agent_messages` (reused, not duplicated — it already had exactly the right shape and an index built for this exact query) and locked down: no client-writable path at all (030's `forbid_client_write` STATEMENT trigger, reused unchanged), `record_agent_turn()` (security definer) the only writer, called once after a real OpenAI reply exists rather than the old two inserts split across the call. Both the OpenAI context and each UI surface's own "restore on load" read use the same `AGENT_CONTEXT_WINDOW` (20, `shared/constants/agent.ts`) most recent rows — an explicit, documented cost/speed tradeoff (memory beyond roughly the last ~10 exchanges is deliberately sacrificed), not an oversight. The client-supplied `history` field is gone entirely (schema, client helper, all 4 call sites) — the server derives its own context, never trusts a client-sent transcript. Both chat surfaces (`FloatingAgent`, `AgentChat`) now seed from the same persisted history — each still keeps its own read, so the "one continuous thread across surfaces" product question stays open (not part of this fix), but neither surface goes blank on reload/navigation anymore. | Done. |
| 18 | A mid-page language switch does not reach the floating agent until reload | Low | `shared/hooks/useLang.ts`, `features/agent/components/FloatingAgent.tsx`, `features/lms/components/LessonView.tsx` | `useLang` persists to localStorage but each component instance reads it once, in a mount-time `useEffect`, and there is no subscription between instances. Verified live: with the lesson player switched to EN (`wow.lang = "en"`, lesson labels reading "Listen"/"Pronounce"), the floating agent's own labels stayed Arabic; after a reload it correctly showed "Open your agent". So the language is right on every fresh page load and only lags a switch made while the page is open. Same root cause as #13 — `useLang` is per-instance state pretending to be app-wide state. | Small-Medium — one shared source of truth (a `LangProvider` context in a client wrapper, or a `storage`/custom-event subscription inside `useLang`) so every instance re-renders on a change. Fixing it once resolves #13 and #18 together. |
| 19 | The lesson-context truncation path has never run on real data | Low (but do not assume it works) | `features/lms/services/lesson.service.ts` (`getLessonAgentContext`, `clip`) | The caps that keep the agent's per-message lesson block bounded (1200 chars per AR/EN body, 600 for the grammar explanation, 25 vocabulary pairs) are **not exercised by any lesson that exists today**. Measured against all 19 real lessons in the DB at the time it shipped: longest AR body 415, longest EN body 557, longest grammar explanation 317, largest vocabulary list 5 — every value far below its cap. So `clip()`, the `…` suffix, the `truncated` flag, and the prompt line telling the agent to admit it can't see the rest are all verified by **code reading only**, never by a live request. Recorded explicitly so nobody later reads "there are caps" as "the caps have been tested". The risk is not today's content but the first genuinely long lesson authored after this. | Small — either a unit test over `clip()` plus a fixture-sized context (the repo already has vitest), or a one-off live check against a deliberately over-length draft lesson, whichever comes first. Worth doing before the language layer's remaining writing phase adds longer lesson bodies. |
| 20 | ~~`profiles.points`/`level` self-awardable via PostgREST~~ **CLOSED (migration 027)** | ~~critical~~ resolved | `supabase/migrations/027_points_award_hardening.sql` | Points/level are no longer writable from any client session — a direct `PATCH` is refused with `42501`. Payouts go through two security-definer functions that each verify one real event and refuse to pay twice for it: `award_lesson_points(lesson_id)` checks the `lesson_progress` row, `award_quiz_points(attempt_id)` (013, extended not forked) checks a real passed attempt on either the assessor or auto branch. Deliberately NOT a generic `award_points(reason)`: that would have been a worse hole than the one being closed — repeatable minting of `PMP_LEVEL_COMPLETE` instead of a one-time set. Verified live: a real lesson completion moved points 10 → 20; four direct PATCH shapes were refused 403/42501 with values unchanged on independent read; replaying the RPC on an already-paid lesson returned `false` with no change; the assessor path still paid out (a second account 0 → 20 on a genuinely passed attempt). | Done. |
| 21 | ~~`roles.assign` has no write path at all~~ **CLOSED (migration 031)** | ~~Medium~~ resolved | `supabase/migrations/031_assign_role.sql`, `app/admin/roles/`, `app/api/admin/roles/route.ts` | `assign_role(p_user, p_role)` (security definer, same shape as `update_pricing_unit`) verifies `has_permission('roles.assign')`, plus `has_permission('roles.assign_super')` for a super_admin target — belt-and-suspenders with the pre-existing `trg_guard_super_admin` (003), which still fires on the UPDATE itself regardless of caller. A new profiles SELECT policy, scoped to the same `roles.assign` permission, lets the screen list users to assign roles to; it ORs with the existing owner-only policy rather than replacing it. Reused the existing `roles.assign`/`roles.assign_super` permissions — no new permission introduced. Verified live: a real `admin` account promoted another account to `moderator` (persisted, `audit_log` row written); the same account attempting to grant `super_admin` was refused **403/42501** (`"Only a super_admin can assign super_admin"`), target role unchanged; an unprivileged account attempting to self-promote to `admin` via the same RPC was refused **403/42501** (`"Not authorized to assign roles"`); the new read policy correctly narrowed back to owner-only the moment the test account's own role was reverted to `user`. | Done. |
| 22 | `lessons.content->...->coin_cost` is dead data still present in every lesson row | Low | `lessons.content` (009, 023), `features/lms/services/lesson.service.ts` | Since 024 the coin cost is read from `pricing_units` by action type; the numbers still sitting in the lesson jsonb decide nothing — not display, not charging — and `resolveLanguageTask` no longer reads them. They were left in place deliberately (no destructive migration over authored content, and historical `language_task_submissions` rows keep their own snapshot regardless). The risk is purely that a future reader trusts them: they will silently disagree with reality the first time an admin changes a price. | Small — a cleanup migration stripping the `coin_cost` keys once the pricing table has been live long enough to be trusted. Explicitly NOT now, by owner decision. |
| 23 | `shared/lib/logger.ts` still says `audit_log` is a TODO, and it is still the *only* table anything audits to | Low | `shared/lib/logger.ts`, `audit_log` (003) | 024 made the pricing functions `audit_log`'s first real writers, so the table is no longer dead — but the stdout-only `auditLog()` helper in `logger.ts` still carries a comment saying a real trail "should" exist, and no other staff action (role grants, content-review decisions in 015b/c, wallet credits) writes a row. RBAC.md's rule that *every* staff action writes to `audit_log` is therefore still aspirational rather than true. | Small per call site — have `auditLog()` insert through a security-definer function (the 024 functions show the shape) and add calls at the remaining staff-action sites, starting with the curriculum-review decisions.
| 24 | ~~A write refused by "no RLS policy" returns **200/204 with zero rows** instead of an explicit refusal~~ **CLOSED (migration 030)** | ~~Low~~ resolved | `supabase/migrations/030_forbid_client_writes_pricing_placement.sql` | A row-level (`FOR EACH ROW`) trigger — the 026 pattern — does **not** work for this case: with no write policy at all, RLS filters every row to zero *during the scan*, before any row-level trigger is ever invoked, so the trigger would never fire. The fix is a **`FOR EACH STATEMENT`** trigger, which fires once per statement unconditionally, before RLS row-filtering happens. `forbid_client_write()` raises `42501` for `current_user in ('authenticated','anon')` and is deliberately SECURITY INVOKER (not DEFINER — 026's own lesson), so `update_pricing_unit`/`consume_placement_quota` (both SECURITY DEFINER, running as the function owner) pass through untouched. Verified live: the exact original attack replayed — `PATCH pricing_units`, `PATCH`/`DELETE`/`INSERT placement_usage` from an unprivileged session — now all return **403** with a named reason (`"Direct UPDATE on pricing_units is not permitted — this table is only written by a verified server-side function"`), and an independent read confirmed the data never changed. No regression: `consume_placement_quota()` still incremented a real counter 0→1, and `update_pricing_unit()` still moved a real price 3→99→3, both from genuinely privileged sessions. | Done. |
| 25 | ~~The full quiz answer key is readable by any enrolled student over PostgREST~~ **CLOSED (migration 028)** | ~~critical~~ resolved | `supabase/migrations/028_quiz_answer_key_isolation.sql` | The key moved to `quiz_answer_keys` (RLS on, **zero policies** — no client role can read it) and was **physically removed** from the `question` jsonb, so "correct_index is never sent to the client" is now a property of the data, not of a route that could be bypassed. Scoring moved into `submit_quiz_attempt()`, a security-definer function that compares internally and returns only an aggregate score — no per-question breakdown, so the key cannot be inferred by diffing attempts. Enrollment and the one-attempt rule moved into the same transaction as the insert, closing a race the route had. Verified live: a direct read of `quiz_answer_keys` returns `[]`; all 18 question objects now contain exactly `options,text`; the embedded-join bypass (`select=id,quiz_answer_keys(correct_index)`) returns `null`; correct answers through the real route still score **100** and wrong answers score **0.00**; the hybrid assessor path still paid a fresh account **0 → 20** on the approved 100% attempt while the unapproved 0% attempt stayed at 0. | Done. |
| 26 | ~~`instructor`/`mentor`/`assessor` were self-grantable with zero verification~~ **CLOSED (migration 034)** | ~~critical~~ resolved | `supabase/migrations/034_grant_capability.sql`, `app/admin/roles/`, `app/api/admin/capabilities/route.ts`, `features/profile/components/ActivateCapabilityButton.tsx` | Found while live-testing migration 032: the same broad "Own capabilities: manage" policy meant for `learner`/`job_seeker`/`freelancer`/`client` let any account self-grant `assessor` (or `instructor`/`mentor`) with one PostgREST call and immediately gain real authority to grade other users' quizzes. Confirmed before writing any code that no legitimate alternative existed — `earner_profiles.verification_status`/`assessor_calibration_results` (008) are pure dead schema, read/written by nothing in `app/` or `features/`; every real authority check in this codebase only ever checks `user_capabilities` membership. `grant_capability(p_user, p_capability)` (security definer, same shape as `assign_role`/031) checks `has_permission('users.manage')` — confirmed against `role_permissions` before choosing it, not assumed — and is the only path to the three privileged capabilities; the four self-service ones are unaffected. `/admin/roles` gained a second, independently-gated section rather than a new page. Verified live: self-grant of all three privileged capabilities refused **403/42501** for a plain account, with an independent read confirming none landed; the four self-service capabilities still granted identically to before; a real `admin` account granted `assessor` to another account via `grant_capability`, landed on the target's own independent read, and wrote a real `audit_log` row; and — negative control — the same admin session was still refused attempting the identical grant as a **direct** table write, proving the function is the only door even for a caller who already holds the permission. | Done. |
| 27 | ~~8 of 17 pages still hardcode `const lang = "ar" as const`~~ **CLOSED for 3, reduced scope for 5** | ~~Medium~~ Low | `app/admin/pricing/page.tsx`, `app/admin/roles/page.tsx`, `app/join/[code]/page.tsx` (full), `features/admin/components/AdminPricingPageContent.tsx`, `features/admin/components/AdminRolesPageContent.tsx`; `app/assessor/queue/page.tsx`, `app/instructor/courses/page.tsx`, `app/instructor/courses/[id]/page.tsx`, `app/instructor/courses/[id]/sessions/page.tsx`, `app/instructor/review/page.tsx` (light), `features/lms/components/AssessorQueuePageContent.tsx`, `features/instructor/components/Instructor*PageContent.tsx` | Split by owner decision into two depths. **Full** (`/admin/pricing`, `/admin/roles`): their client views already took `lang` as a plain prop (no independent `useLang()`), so these got the exact five-page shape — `getServerLang()` + one `useLang(initialLang)`+`LangToggle` owner passing `lang` straight through. `/join/[code]` is a one-line `getServerLang()` swap (pure redirect page, no persistent content to toggle). **Light** (the remaining 5): their sub-components (`CreateCourseForm`, `CurriculumContributionSection`, `CourseContentManager` — 3 independent instances in one file — `InviteLinkCard`, `SessionsManager`, `ReviewQueue`, `AssessorQueue`) each already ran their own independent `useLang("ar")`, unlike the five originally-migrated pages (which had been consolidated to one `useLang()` per page as part of that same migration). Converting all of them to prop-driven `lang` was judged bigger than "small per page" once actually surveyed — deferred by owner decision. What shipped instead: only the page shell (Logo/title/breadcrumbs/`LangToggle`) reads `getServerLang()`/reacts live; each sub-component keeps self-correcting to the cookie on its own mount, same accepted flash-then-correct gap as #18, not a new one. | Remaining: converting the 5 light-group components to prop-driven `lang` (removing their independent `useLang("ar")` calls) so a toggle click reaches them instantly instead of on next mount — same shape as #18's own fix, whenever that's picked up. |
| 28 | ~~`/courses` course cards render title/summary in whatever language they were authored in~~ **CLOSED (migration 035_courses_translations)** | ~~Low~~ resolved | `supabase/migrations/035_courses_translations.sql`, `features/lms/services/course.service.ts` (`resolveCourseText`), `features/lms/components/CourseCatalog.tsx`, `features/lms/components/CourseDetailView.tsx` | Mirrors `lessons.translations` (008) exactly rather than inventing a new shape: `courses.translations jsonb not null default '{}'::jsonb`, `{ar: {title, summary}, en: {...}}`, with the existing `title`/`summary` columns kept as the canonical/fallback values — `resolveCourseText(course, lang)` picks `translations[lang]` and falls back to the canonical columns when a language is missing, same two-level fallback `LessonView` already used for `lesson.translations`. No RLS change needed: "Courses: user-owner manages"/"org staff manage" (004) are row-level, already covering the new column. Verified live: PATCHing `translations.en.title` on an owned course made `/courses/[id]` render that English title with the toggle on `en`, and correctly fell back to the canonical Arabic title on `ar` (translations.ar left empty) — both directions confirmed, not just the write. `description`/`language` (both dead — confirmed via live query: `description` is `null` on all 3 real rows and never selected by any query; `language` is selected but never read anywhere) were deliberately left in place, same call as #22's `coin_cost`. | Done for the schema/resolution mechanism. Still open: an actual translation-authoring pass to populate `translations.en` on real course content — this migration only adds the capability. |

## تحذير: محادثات تحديد المستوى المهجورة (migration 022, 2026-07-25)

محادثة تحديد المستوى مجانية وغير مكررة **بعد اكتمالها فقط** — حارس المرة الواحدة (المفتاح الأساسي في `user_language_profiles`) لا يمنع من يبدأ المحادثة ويهجرها قبل إنتاج الوكيل لكتلة التقييم من إعادة البدء بلا حد، وكل رسالة استدعاء OpenAI حقيقي بتكلفة حقيقية وبلا أي مقابل كوينز. الحارس الوحيد حالياً محدد معدل في الذاكرة (15 رسالة / 10 دقائق) **يسقط كلياً عند إعادة تشغيل الخادم ويتضاعف بعدد الـinstances على serverless**. يجب حل حقيقي (عدّاد محاولات مخزَّن في قاعدة البيانات أو حد يومي دائم) **قبل أي حركة مرور Beta حقيقية تتجاوز دائرة الاختبار المغلقة** — نفس درجة إلزامية تحذير الشراء المحاكى أعلاه.

## ~~تحذير: الشراء المحاكى بلا حد~~ — مُغلَق (2026-07-29)

المالك اتخذ قراراً واعياً ومؤجَّلاً عمداً: تفعيل شراء كوينز **محاكى محلياً بالكامل** (`credit_coins()`، مسار `/api/wallet/purchase`) لأغراض الاختبار، بلا بوابة دفع حقيقية وبلا أي حد لتكرار الشراء. تحقّق حي أثناء الاختبار: نفس الحساب اشترى نفس الباقة ثلاث مرات متتالية بنجاح (25 → 325 → 625 → 925 كوينز)، كل عملية سجّلت صفاً حقيقياً في `coin_transactions`.

**أُغلق باختيار «تعطيل المسار كلياً» من الخيارات الثلاثة التي كان هذا البند يطرحها.** `/api/wallet/purchase` صار **مُعطَّلاً افتراضياً** ولا يعمل إلا إذا كان `WALLET_SIMULATION_ENABLED === "true"` صراحةً. متغير مفقود أو مكتوب خطأً **يفشل مغلقاً** — الاتجاه الصحيح لشيء يوزّع عملة مجاناً. الفحص أول سطر في المسار، **قبل التحقق من الجلسة**، لأن «هل الميزة موجودة أصلاً» ليس سؤالاً خاصاً بمستخدم؛ والرد **503 لا 403** لأن القدرة غائبة والمستخدم لم يخطئ. **تحقق حي للفرعين**: بلا المتغير → **503** برسالة عربية واضحة، والرصيد بقي **10** بلا أي صف جديد في `coin_transactions` (لا سكّ عملة)؛ ومع `WALLET_SIMULATION_ENABLED=true` → **200**، الرصيد `10 → 310` وصف حقيقي `+300 simulated_purchase` — لا انحدار في بيئة التطوير. الواجهة تعرض إشعاراً واضحاً وتعطّل أزرار الشراء عند التعطيل، **لكن الحماية الحقيقية في المسار** والواجهة عرض فقط. **ما لم يُحَل**: هذه ليست بوابة دفع ولا تدّعي ذلك — الشراء الحقيقي عمل منفصل لاحق؛ وعند تفعيل المحاكاة محلياً ما زال لا يوجد حد على التكرار، وهو مقبول فقط لأنها لم تعد قابلة للتفعيل في الإنتاج.

## Already resolved this sprint (for the record, not open debt anymore)
- Missing `tsconfig.json` / `next.config.js` / `next-env.d.ts` / `.eslintrc.json` — project would not have built. **Fixed.**
- Client-controlled point amount (critical security hole). **Fixed.**
- No FK indexes on `enrollments`, `user_badges`, `ai_conversations`, `horizon_progress`. **Fixed (migration 002).**
- `profiles.updated_at` never actually updated. **Fixed (migration 002 trigger).**
- No `app/error.tsx` / `app/not-found.tsx`. **Fixed.**

## Already resolved in Sprint 3
- Nova's conversation `history` trusted from client with only a TS type
  — resolved back in Sprint 1.5 (`agentRequestSchema`/`novaRequestSchema`
  zod validation) but never removed from this table until now.
- Points-awarding had no caller that verified the underlying event
  actually happened — `app/api/lms/lessons/complete` and
  `app/api/lms/quizzes/submit`/`grade` now all verify server-side
  (RLS-gated reads / a real graded attempt) before calling
  `awardPoints`/`award_quiz_points`. **Fixed.**
- Five real RLS write-permission gaps found via two-real-account
  acceptance testing (`entity_skills`, `skill_evidence`,
  `quiz_attempts` UPDATE, `system_actors` grant, ambiguous `profiles`
  embed) — see migrations 010, 012, 013 and ROADMAP.md Sprint 3.
  **Fixed.**
