NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
OPENAI_API_KEY=your-openai-key

# Locally-simulated coin purchases (no real payment gateway).
# MUST stay unset/false in production — "true" lets any signed-in user
# mint spendable coins by clicking Buy, with no limit on repeats.
WALLET_SIMULATION_ENABLED=true
