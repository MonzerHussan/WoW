# CLAUDE.md — تعليمات جلسات Claude Code لمشروع WOW

> اقرأ هذا الملف أولًا في كل جلسة. هو نقطة الدخول لفهم المشروع كاملًا.

## ما هذا المشروع

**WOW (World of Work)** — منصة تعليمية تأهيلية توظيفية ثنائية اللغة (AR/EN):
تعليم → توظيف → ترقية، مع وكيل ذكاء اصطناعي شخصي **بلا اسم مفروض من
النظام** (المستخدم يختاره بنفسه أول تفاعل، بقيمة مبدئية "رفيق" قبل
الاختيار — لا تُسمِّه Nova في أي نص جديد، كانت التسمية الأصلية
واستُبدلت عمدًا، انظر القسم 2 أدناه)، ونظام Career DNA (توأم رقمي
مهني)، ومحرك أدلة للمهارات، وعقود تشغيل خارجي بضمان المنصة عبر شريك
مرخّص.

المالك: MonzerHussan — المستودع المستهدف: `https://github.com/MonzerHussan/WoW`

## اقرأ بهذا الترتيب قبل أي عمل

1. `README.md` — التشغيل والإعداد
2. `ROADMAP.md` — ما أُنجز في كل سبرنت وما هو التالي (**المرجع الأول للحالة**)
3. `PROJECT_STRUCTURE.md` — البنية (features/ + shared/) وقواعد الاستيراد
4. `CODING_GUIDELINES.md` — قواعد الكود الملزمة (i18n، zod، أمان النقاط...)
5. `RBAC.md` — نموذج الأدوار والصلاحيات الكامل
6. `DOMAIN_CONTRACTS.md` — العقود الملزمة + ميثاق الشفافية T1–T9
7. `SECURITY.md` و`TECH_DEBT.md` — ما أُصلح وما هو مفتوح

## الحالة الحالية

هذا القسم كان يصف حالة المشروع في مرحلة الإعداد الأولى (Sprint 0، قبل
أي migration، قبل أول push لـGitHub) — أصبح متأخرًا جدًا عن الواقع
(74 migration مطبَّقة على الإنتاج، منصة منشورة فعليًا على Vercel، عشرات
الشاشات). **الحالة الحقيقية دائمًا في `ROADMAP.md`** — هو المرجع الأول
كما يقول القسم أعلاه بالفعل؛ لا تُبقِ نسخة ثانية من "الحالة الحالية"
هنا تتيح لها التصادم مع ROADMAP.md وتضليل جلسة قادمة (نفس الفجوة التي
كشفها تدقيق 2026-08-15 بين ROADMAP.md والواقع، على نطاق أصغر).

## أوامر أساسية

```bash
npm install          # الاعتماديات
npm run dev          # تشغيل محلي
npm run test         # vitest (اختبارات وحدة)
```

## البوابات الأربع قبل كل التزام — بلا استثناء

```bash
npx tsc --noEmit     # 1
npm run lint         # 2
npm run build        # 3
npm run smoke        # 4  ← يفتح الصفحات فعلًا
```

**أربع لا ثلاث.** البوابات الثلاث الأولى تتحقق من أن الكود **يُترجَم**؛
ولا واحدة منها تتحقق من أن الصفحة **تُفتح**. الرابعة تفعل ذلك وحدها.

**لماذا أُضيفت (قرار المالك، 2026-08-20):** خطأ تمرير دالة عبر حدّ
Server→Client شُحن **مرّتين** (`245f4f3` فأعطب `/profile`، ثم
2026-08-20 فأعطب `/admin/roles` بـ**500**). في الحالتين مرّت
`tsc` و`lint` و`build` كلها. والقاعدة كانت **موثّقة** في المرّتين. أي
أن السقوط كان في **الانضباط لا في الأداة** — ولذلك `smoke` بوابة ثابتة
لا سكربتًا اختياريًا: سكربت يُعتمد على تذكّره ضمانة أضعف من بوابة
تُشغَّل بلا تذكير. التفصيل الكامل والنمط الصحيح في
`CODING_GUIDELINES.md §5a`.

**`npm run smoke` يفتح المسارات المحميّة بجلسة حقيقية**، لأن معظم صفحات
المنصة تنتهي بـ`redirect()` للزائر غير المسجَّل — ففحص بلا مصادقة كان
سيُرجع `307` ولا يبلغ الشيفرة المعطوبة أصلًا (وهذا ما حدث حرفيًا:
`/admin/roles` أعطى `307` بلا جلسة و`500` بجلسة). يقرأ `.env.local`
وحساب اختبار من `SMOKE_TEST_EMAIL`/`SMOKE_TEST_PASSWORD`، ويتخطّى
المسارات المحميّة بتحذير إن لم تُضبط — **ولا يفشل صامتًا**.

في CI يعمل الشقّ العام فقط (`npm run smoke:public`)، لأن CI يعمل بمفاتيح
Supabase وهمية فلا جلسة ممكنة فيه. تغطية المسارات المحميّة في CI تحتاج
مشروع Supabase اختباريًا — **مربوطة بـ`TECH_DEBT #6`، وليست بندًا
جديدًا**.

## قواعد غير قابلة للكسر (ملخص — التفصيل في CODING_GUIDELINES.md)

1. **اتجاه الاستيراد**: `app → features → shared` فقط. الميزات لا تستورد
   من ميزات شقيقة (التركيب المشترك يحدث في صفحات app/).
2. **لا نصوص AR/EN مكتوبة داخل المكونات** — كل النصوص في
   `shared/i18n/dictionary.ts` عبر `t()` أو `useLang()`.
3. **كل مدخل API يُتحقق منه بـ zod** من `shared/schemas/` قبل لمس
   Supabase أو OpenAI.
4. **النقاط لا تُقبل من العميل أبدًا** — فقط `reason` يُبحث في
   `shared/constants/points.ts` (كانت ثغرة حرجة وأُصلحت — لا تُعِدها).
5. **الأسرار خادمية فقط** — لا `NEXT_PUBLIC_` على أي مفتاح سري.
6. **تغييرات قاعدة البيانات = migration جديد مرقّم** في
   `supabase/migrations/` — لا تعدّل ملفات migrations القديمة أبدًا.
   **والملف يُكتب في المستودع أولًا، ثم يُطبَّق منه** — انظر القسم
   التالي، وهو غير قابل للتفاوض.
7. **ميثاق الشفافية T1–T9** (في DOMAIN_CONTRACTS.md) ملزم: كل درجة لها
   تفسير إلزامي، بيانات الشخصية لا تُشارك مع المنظمات أبدًا، الموافقة
   لكل منظمة على حدة، الدرجات ترشّح ولا ترفض آليًا.
8. **الوثائق تتحدث مع الكود**: أي تغيير معماري/أمني/أدوار يحدّث الوثيقة
   المقابلة في نفس الـ commit.

## ترتيب الترحيلات: الملف أولًا، ثم التطبيق منه — قاعدة غير قابلة للتفاوض

**الملف `supabase/migrations/NNN_*.sql` يُكتب في المستودع أولًا. ثم
يُطبَّق محتواه على Supabase منه — بنسخه من الملف كما هو.**

**ممنوع**: كتابة SQL في محرر Supabase مباشرةً ثم إنشاء الملف بعده،
بأي صيغة — لا «سأكتبه بعد أن ينجح»، ولا إعادة بناء الملف لاحقًا من
`pg_get_functiondef` أو `pg_policies` أو أي قراءة من القاعدة. إعادة
البناء تُنتج ملفًا يبدو صحيحًا وقد يكون كذلك، لكنها **تصحيح لخرقٍ وقع
بالفعل**، لا التزام بالقاعدة.

**لماذا هذه القاعدة تحديدًا، ولماذا في هذا الملف بالذات:** كل بند آخر
هنا يحمي من كتابة كودٍ خاطئ — وهذا يمكن إصلاحه دائمًا. هذه القاعدة وحدها
تحمي من **فقدٍ لا يُسترد**: SQL طُبِّق على الإنتاج ولم يصل المستودع
ليس خطأ يُصحَّح لاحقًا، بل حالةُ قاعدة بيانات **لا مصدر لها**. لو
انقطعت الجلسة أو ضاع سياقها قبل كتابة الملف، لا يبقى أثر لما طُبِّق إلا
القاعدة نفسها. ولذلك هي مكتوبة هنا في `CLAUDE.md` الذي **يُقرأ في
بداية كل جلسة**، لا في `TECH_DEBT.md` الذي لا يُقرأ إلا عند البحث.

**وقعت مرّتين خلال أربعة أيام — والثانية بعد اكتشاف الأولى وإصلاحها:**

| التاريخ | الحادثة |
|---|---|
| **2026-08-15** | تدقيق الحالة كشف أن **34 ترحيلًا وكل كود التطبيق** لم تُدفع إلى GitHub منذ 2 أغسطس، ومنها **071–074 مطبَّقة على الإنتاج وغائبة عن المستودع**. كان **أعلى بند أولوية في التقرير كله**، لأنه الوحيد الذي يسبّب فقدًا لا يُسترد. |
| **2026-08-20** | **الترحيل 078** طُبِّق على الإنتاج، ثم أُنشئ ملفه بعده بإعادة بنائه من الكائنات المطبَّقة (`pg_get_functiondef` / `pg_policies` / `pg_constraint`). نفس الخرق، بعد أربعة أيام من اكتشافه. |

**أن يُكتشف الخرق ويُصلَح لا يمنع الثالثة. الترتيب هو ما يمنعها.**

## السبرنت التالي المتفق عليه: Sprint 3 — LMS + الوكيل الشخصي + الملف الشخصي + تغذية DNA

المجال الكامل جاهز (migrations 004-006-007-007b-008، نُفِّذت جميعها على
Supabase). المطلوب بناء الواجهات والخدمات، **بهذا الترتيب بالضبط**
(كل مرحلة أساس لما بعدها):

### 1. `features/lms/` (الأساس)
كتالوج الدورات (المنشورة فقط للزوار)، صفحة الدورة، مشغّل الدروس، أخذ
الاختبارات. **كل مستوى PMP الآن hybrid إلزاميًا** (008): تصحيح آلي فوري
+ اعتماد بشري نهائي من مقيّم، بمهلة مراجعة 48 ساعة (`review_deadline`).

### 2. `features/agent/` (الوكيل الشخصي — ليس Nova بعد الآن)
- `user_agent_profiles` (007b): صف واحد لكل مستخدم، ذاكرة موحّدة عبر كل
  قدراته — **لا وكيل منفصل لكل دور**.
- أول محادثة: المستخدم يختار اسم وكيله بنفسه (`chosen_name`). **لا اسم
  مفروض من النظام، ولا تعرض "Nova" كاسم افتراضي ظاهر للمستخدم** — استبدل
  أي إشارة متبقية له في الواجهة بحقل اختيار الاسم عند أول تفاعل.
- الوكيل "يفعل لا يتحدث فقط": يقرأ DNA كامل (مهارات، تسجيلات، درجات)
  ويكتب توصيات فعلية في `career_recommendations`، لا ردودًا عامة فقط.
- أسلوبه يتكيّف حسب القدرة النشطة (طالب/موظف/مستقل...) — هويته لا تتغيّر.
- **الوكيل يبقى مجانيًا الآن** (محميًا فقط بـ rate limit التقني الحالي).
  **لا تربط `spend_coins()` بـ`/api/nova` بعد** — يُفعَّل لاحقًا في سبرنت
  الاشتراكات مع بوابة دفع فعلية.

### 3. `features/profile/` (صفحة `/profile` — إلزامية هذا السبرنت)
تجمع بصريًا: محاور Career DNA، المهارات وأدلتها (`skill_evidence`)،
الشهادات، Trust Score وEmployability Score بتفسيرهما الكامل
(`{factors:[{name,weight,value,tip}]}`)، القدرات النشطة
(`user_capabilities`) مع زر "فعّل قدرة جديدة" (**لا يحذف أي قدرة قائمة
أبدًا**)، والوكيل الشخصي مع آخر توصياته ونتائجها.

### 4. تفعيل عقد LMS→DNA (البند §5 في DOMAIN_CONTRACTS.md)
- إكمال درس → `lesson_progress` + نقاط `LESSON_COMPLETE` (تحقق خادمي
  من الإكمال الفعلي قبل منح النقاط — TECH_DEBT #10)
- اجتياز اختبار (**بعد اعتماد المقيّم**، لأن كل الاختبارات hybrid الآن) →
  `entity_skills(source='assessment')` + `skill_evidence('quiz_attempt')`
  تلقائيًا
- إصدار شهادة → `certificates` + إعادة حساب Employability
- كل إعادة حساب درجة = **صف جديد** في `career_scores` (سلسلة زمنية، لا
  تحديث في المكان) مع `explanation` إلزامي

### بنية المحتوى (008) — غير إلزامية الآن
`lessons.translations` (jsonb، AR/EN)، `content_review_votes` لحوكمة
النشر (owner + peer_assessor + nova_check gate) — ليست ضرورية لبناء
مشغّل الدروس الأساسي، لكن اربط بها إن بُنيت أي واجهة لإدارة المحتوى.

### قاعدة تسمية صارمة: نقاط ≠ كوينز
`profiles.points` ("نقاط"/gamification) و`wallets` ("كوينز"، 007b)
**نظامان منفصلان تمامًا** في كل نص/متغير/مفتاح i18n. لا تُترجم "Coins"
إلى "نقاط" أبدًا في أي مكان، ولا العكس.

### مهمتان محمولتان (منفصلتان عن الترتيب أعلاه)
(أ) middleware يعامل `profiles.status != 'active'` كغير مسجّل؛
(ب) نقل rate limiter من الذاكرة لمخزن مشترك (Upstash) قبل توسيع الوكيل.

## موانع إطلاق مسجّلة (لا تُطلق المنصة للجمهور قبلها)

- سياسة القاصرين/موافقة ولي الأمر (RBAC.md)
- توقيع اتفاقية مع شريك توريد عمالة مرخّص فعلي قبل تفعيل عقود
  `outsourcing` (DOMAIN_CONTRACTS.md §7)
- استشارة قانونية محلية لصياغة نصوص الضمان التجارية

## عند الشك

- الحالة والأولويات: `ROADMAP.md`
- أين يوضع الكود: `PROJECT_STRUCTURE.md`
- هل هذا مسموح: `CODING_GUIDELINES.md` ثم `DOMAIN_CONTRACTS.md`
- ولا تكسر بناءً يعمل: `npm run build` قبل كل commit.
